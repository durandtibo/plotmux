{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"#overview","title":"Overview","text":"<p><code>plotmux</code> is a lightweight abstraction layer over Python's plotting libraries. Instead of writing your visualization code against a specific library like Matplotlib, you write it once against <code>plotmux</code>'s unified API, and choose the rendering backend at runtime.</p> <p>This means you can prototype a figure with a fast, familiar backend, then switch to another one for interactive dashboards or publication-quality output, without touching your plotting code. Swapping backends is a one-line configuration change.</p> <p>Quick Links:</p> <ul> <li>User Guide</li> <li>Installation</li> <li>Features</li> <li>Contributing</li> </ul>"},{"location":"#why-plotmux","title":"Why plotmux?","text":"<p>Writing plotting code directly against Matplotlib, or any other single library, ties every chart in your codebase to that library's API. Switching backends later, e.g. to try an interactive one, means rewriting every call site. <code>plotmux</code> solves this with a small, backend-agnostic API:</p> <p>Plot with the default backend:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 2, 3, 3, 3], bins=3)\n&gt;&gt;&gt; fig.save(\"histogram.png\")  # doctest: +SKIP\n</code></pre> <p>Switch backends without changing the plotting code:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; with plotmux.backend(\"xy\"):  # doctest: +SKIP\n...     fig = plotmux.line([1, 2, 3], [1, 4, 9])\n...\n</code></pre> <p>See the user guide for detailed examples.</p>"},{"location":"#features","title":"Features","text":"<p><code>plotmux</code> provides a small, focused set of utilities for backend-agnostic plotting:</p>"},{"location":"#unified-plotting-api","title":"\ud83c\udfa8 Unified Plotting API","text":"<p>Plot histograms, empirical CDFs, line charts, scatter plots, and bar charts with one API, regardless of the backend that renders them:</p> <ul> <li><code>plotmux.hist()</code> for histograms, with optional binning, density, and quantile-based axis ranges</li> <li><code>plotmux.cdf()</code> for empirical cumulative distribution functions</li> <li><code>plotmux.line()</code> for line charts</li> <li><code>plotmux.scatter()</code> for scatter charts</li> <li><code>plotmux.bar()</code> for bar charts</li> <li>Common figure-level styling (<code>title</code>, <code>xlabel</code>, <code>ylabel</code>, <code>xscale</code>, <code>yscale</code>) shared by every   chart type</li> </ul> <p>Learn more \u2192</p>"},{"location":"#pluggable-backends","title":"\ud83d\udd00 Pluggable Backends","text":"<p>Choose the rendering backend at runtime, and swap it with a one-line change:</p> <ul> <li>Built-in Matplotlib backend</li> <li>Built-in <code>xy</code> backend for interactive charts</li> <li>Built-in Bokeh backend for interactive, standalone HTML charts</li> <li>Built-in Altair (Vega-Lite) backend for declarative,   standalone HTML charts</li> <li>Third-party backends can plug in via a Python entry point, no changes to <code>plotmux</code> required</li> </ul> <p>Learn more \u2192</p>"},{"location":"#layering","title":"\ud83e\udde9 Layering","text":"<p>Combine multiple charts onto one shared set of axes with <code>plotmux.layer()</code>, e.g. overlaying a line on top of a histogram, without leaving the unified API.</p> <p>Learn more \u2192</p>"},{"location":"#grid-layouts","title":"\ud83d\udd32 Grid Layouts","text":"<p>Lay out multiple charts as independent panels in a grid with <code>plotmux.grid()</code>, the backend-agnostic equivalent of Matplotlib's <code>pyplot.subplots()</code>. Panels can themselves be layers, since layering and gridding are independent, composable concerns.</p> <p>Learn more \u2192</p>"},{"location":"#backend-agnostic-colors","title":"\ud83d\udd8c\ufe0f Backend-Agnostic Colors","text":"<p>Use the same color syntax everywhere: hex strings, CSS/Matplotlib named colors (e.g. <code>\"tab:blue\"</code>), or RGB(A) float tuples. <code>plotmux</code> normalizes and validates colors once, then hands each backend its native representation.</p> <p>Learn more \u2192</p>"},{"location":"#export","title":"\ud83d\udcbe Export","text":"<p>Save any figure to a file with <code>Figure.save()</code>. The export format is inferred from the file suffix (e.g. <code>.png</code>, <code>.svg</code>, <code>.pdf</code>), and the parent directory is created automatically.</p> <p>Learn more \u2192</p>"},{"location":"#contributing","title":"Contributing","text":"<p>Contributions are welcome! We appreciate bug fixes, feature additions, documentation improvements, and more. Please check the contributing guidelines for details on:</p> <ul> <li>Setting up the development environment</li> <li>Code style and testing requirements</li> <li>Submitting pull requests</li> </ul> <p>Whether you're fixing a bug or proposing a new feature, please open an issue first to discuss your changes.</p>"},{"location":"#api-stability","title":"API Stability","text":"<p> Important: As <code>plotmux</code> is under active development, its API is not yet stable and may change between releases. We recommend pinning a specific version in your project's dependencies to ensure consistent behavior.</p>"},{"location":"#license","title":"License","text":"<p><code>plotmux</code> is licensed under BSD 3-Clause \"New\" or \"Revised\" license available in LICENSE file.</p>"},{"location":"get_started/","title":"Get Started","text":"<p>We highly recommend installing <code>plotmux</code> in a virtual environment to avoid dependency conflicts.</p>"},{"location":"get_started/#using-uv-recommended","title":"Using uv (recommended)","text":"<p><code>uv</code> is a fast Python package installer and resolver:</p> <pre><code>uv pip install plotmux\n</code></pre> <p>Install with a specific backend:</p> <pre><code>uv pip install plotmux[matplotlib]\nuv pip install plotmux[xy]\nuv pip install plotmux[bokeh]\nuv pip install plotmux[altair]\n</code></pre> <p>Install with all optional dependencies:</p> <pre><code>uv pip install plotmux[matplotlib,xy,bokeh,altair,plotly]\n</code></pre>"},{"location":"get_started/#using-pip","title":"Using pip","text":"<p>Alternatively, you can use <code>pip</code>:</p> <pre><code>pip install plotmux\n</code></pre> <p>Install with a specific backend:</p> <pre><code>pip install plotmux[matplotlib]\npip install plotmux[xy]\npip install plotmux[bokeh]\npip install plotmux[altair]\npip install plotmux[plotly]\n</code></pre>"},{"location":"get_started/#installing-from-source","title":"Installing from source","text":"<p>To install <code>plotmux</code> from source, you can follow the steps below.</p> <p>First, clone the git repository:</p> <pre><code>git clone git@github.com:durandtibo/plotmux.git\ncd plotmux\n</code></pre> <p>Note: <code>plotmux</code> requires Python 3.10 or higher. The <code>xy</code> backend additionally requires Python 3.11 or higher.</p> <p>It is recommended to create a virtual environment (this step is optional). To create a virtual environment, you can use the following command:</p> <pre><code>make setup-venv\n</code></pre> <p>This command automatically creates a virtual environment using <code>uv</code>. When the virtual environment is created, you can activate it with the following command:</p> <pre><code>source .venv/bin/activate\n</code></pre> <p>Then, you should install the required packages to use <code>plotmux</code> with the following command:</p> <pre><code>inv install --docs-deps\n</code></pre> <p>This command will install all the required packages. You can also use this command to update the required packages. This command will check if there is a more recent package available and will install it. Finally, you can test the installation with the following command:</p> <pre><code>inv unit-test --cov\n</code></pre>"},{"location":"get_started/#dependencies","title":"Dependencies","text":"<p><code>plotmux</code> only has two hard dependencies: NumPy and <code>coola</code>. Each rendering backend is an optional dependency, so the core package (specs, registry, configuration, public API) always imports cleanly, even with no plotting library installed. Only the backend you actually use needs to be installed.</p> Extra Installs Notes <code>matplotlib</code> Matplotlib Python 3.10+ <code>xy</code> <code>xy</code> Python 3.11+ <code>bokeh</code> Bokeh Python 3.10+ <code>altair</code> Altair Python 3.10+"},{"location":"get_started/#quick-check","title":"Quick check","text":"<p>Once installed, you can verify the installation with a minimal example:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 2, 3, 3, 3], bins=3)\n&gt;&gt;&gt; fig.save(\"histogram.png\")  # doctest: +SKIP\n</code></pre> <p>See the user guide to learn more about <code>plotmux</code>'s API.</p>"},{"location":"dev/architecture/","title":"Architecture","text":"<p>This page describes <code>plotmux</code>'s internal architecture: how it is organized, and why. See DESIGN.md in the repository for the full, up-to-date design document.</p>"},{"location":"dev/architecture/#goal","title":"Goal","text":"<p><code>plotmux</code> is a lightweight abstraction layer over Python's plotting libraries: users write plotting code once against <code>plotmux</code>'s unified API and choose the rendering backend (<code>matplotlib</code>, <code>xy</code>, <code>bokeh</code>, <code>altair</code>, <code>plotly</code>, ...) at runtime. Swapping backends should be a one-line configuration change, and adding a new backend or chart type should not require changing existing code.</p> <p>The unified API targets a small set of generic, broadly-useful chart types and figure-level concerns: the ones almost every plotting task needs (histograms, empirical CDFs, line charts, scatter plots, bar charts, layering them together, laying them out in a grid, common axis styling, per-mark color, export), not comprehensive coverage of every chart type a backend can draw. A niche or highly backend-specific plot is expected to stay behind the escape hatch (<code>Figure.to_native()</code>) rather than becoming a new spec.</p>"},{"location":"dev/architecture/#principle-separate-spec-from-render","title":"Principle: Separate Spec From Render","text":"<p>Two layers:</p> <ol> <li>Chart specs (<code>plotmux.specs</code>): plain, backend-agnostic frozen dataclasses describing what    to plot (data + encoding + style). They never import a plotting library.</li> <li>Backends (<code>plotmux.backends</code>): one package per plotting library, responsible for turning a    spec into that library's native figure object and for exporting it to a file.</li> </ol> <p>This mirrors the Vega-Lite/Altair split, and is what makes \"swap backend in one line\" true: because a spec cannot hold a Matplotlib <code>Axes</code> or an <code>xy.Chart</code>, switching backends can never leak library-specific state back into user code.</p>"},{"location":"dev/architecture/#package-layout","title":"Package Layout","text":"<pre><code>src/plotmux/\n\u251c\u2500\u2500 utils/\n\u2502   \u251c\u2500\u2500 range.py                  # find_range(): quantile-or-explicit axis bounds\n\u2502   \u251c\u2500\u2500 cdf.py                    # compute_cdf_steps(): binned empirical CDF step vertices\n\u2502   \u2514\u2500\u2500 imports/                  # one module per optional backend dependency\n\u2502                                  # (is_matplotlib_available(), is_xy_available(),\n\u2502                                  #  is_bokeh_available(), is_altair_available(),\n\u2502                                  #  is_plotly_available())\n\u251c\u2500\u2500 colors/\n\u2502   \u251c\u2500\u2500 parser.py                 # parse_color(): canonical RGBA normalization\n\u2502   \u251c\u2500\u2500 palette.py                # PRIMARY/SECONDARY/TERTIARY, DEFAULT_PALETTE\n\u2502   \u2514\u2500\u2500 named.py                  # static CSS/matplotlib named-color table\n\u251c\u2500\u2500 specs/\n\u2502   \u251c\u2500\u2500 base.py                   # BaseSpec (title/xlabel/ylabel/xscale/yscale)\n\u2502   \u251c\u2500\u2500 histogram.py              # HistogramSpec\n\u2502   \u251c\u2500\u2500 cdf.py                    # CdfSpec\n\u2502   \u251c\u2500\u2500 line.py                   # LineSpec\n\u2502   \u251c\u2500\u2500 scatter.py                # ScatterSpec\n\u2502   \u251c\u2500\u2500 bar.py                    # BarSpec\n\u2502   \u251c\u2500\u2500 layer.py                  # LayerSpec (rejects nesting + empty layers)\n\u2502   \u2514\u2500\u2500 grid.py                   # GridSpec (rejects nesting + empty cells)\n\u251c\u2500\u2500 backends/\n\u2502   \u251c\u2500\u2500 base.py                   # Backend ABC + dispatch helpers\n\u2502   \u251c\u2500\u2500 registry.py                # register_backend() / get_backend() / entry points\n\u2502   \u251c\u2500\u2500 matplotlib/                # MatplotlibBackend\n\u2502   \u251c\u2500\u2500 xy/                        # XyBackend\n\u2502   \u251c\u2500\u2500 bokeh/                     # BokehBackend\n\u2502   \u251c\u2500\u2500 altair/                    # AltairBackend\n\u2502   \u2514\u2500\u2500 plotly/                    # PlotlyBackend\n\u251c\u2500\u2500 figure.py                     # Figure wrapper\n\u251c\u2500\u2500 export.py                      # save(figure, path)\n\u251c\u2500\u2500 config.py                      # default backend + context manager\n\u251c\u2500\u2500 exceptions.py                  # PlotmuxError hierarchy\n\u251c\u2500\u2500 api.py                         # public hist(), cdf(), line(), scatter(), bar(), layer(), grid()\n\u2514\u2500\u2500 testing/fixtures.py            # pytest fixtures for downstream users\n</code></pre> <p>Every backend (<code>matplotlib</code>, <code>xy</code>, <code>bokeh</code>, <code>altair</code>, <code>plotly</code>) implements all seven specs, including <code>grid.py</code> and <code>cdf.py</code>.</p> <p><code>specs/&lt;type&gt;.py</code> plus one <code>_RENDERERS</code> entry per backend is the shape a new, similarly generic chart type would take. A new backend adds a new <code>backends/&lt;name&gt;/</code> subpackage alongside the existing ones.</p>"},{"location":"dev/architecture/#data-flow","title":"Data Flow","text":"<pre><code>user code\n   \u2502  plotmux.hist(values, bins=30, xmin=\"q0.1\")\n   \u25bc\napi.py             builds a HistogramSpec, resolves the active backend\n   \u2502\n   \u25bc\nspecs               HistogramSpec (frozen dataclass, no plotting import)\n   \u2502\n   \u25bc\nbackends/registry   get_backend(\"matplotlib\") -&gt; MatplotlibBackend\n   \u2502\n   \u25bc\nbackends/matplotlib.backend.render(spec)  -&gt;  native matplotlib Figure\n   \u2502\n   \u25bc\nfigure.py           Figure(spec, backend_name, native)\n   \u2502\n   \u25bc\nuser code           fig.show() / fig.save(\"out.png\") / fig.to_native()\n</code></pre> <p>Backend registration is lazy, not eager: <code>plotmux/__init__.py</code> does not import any of the five built-in backend subpackages (<code>matplotlib</code>, <code>xy</code>, <code>bokeh</code>, <code>altair</code>, <code>plotly</code>) at <code>import plotmux</code> time. Instead, <code>backends/registry.py</code> holds a <code>{name: module path}</code> map, and <code>get_backend(name)</code> imports the matching submodule the first time that name is actually requested (e.g. via <code>backend=\"matplotlib\"</code>, or the first <code>plotmux.hist(...)</code> call after <code>plotmux.set_backend(\"xy\")</code>); each subpackage's <code>__init__.py</code> still calls <code>register_backend(...)</code> as an import-time side effect, guarded by that library's <code>is_*_available()</code> check, only now triggered later. This means a process that only ever renders with <code>matplotlib</code> never imports <code>xy</code>, <code>bokeh</code>, <code>altair</code>, or <code>plotly</code> (or their underlying libraries) even if all four happen to be installed alongside it.</p> <p><code>plotmux/__init__.py</code> additionally calls <code>plotmux.backends.registry.load_entry_point_backends()</code> once, at import time: this imports every third-party backend advertised via the <code>plotmux.backends</code> entry-point group (see Adding a Third-Party Backend). Since none of the five built-ins are imported yet at that point either, a third-party plugin can freely register under any name, built-in or not \u2014 whichever registers last for a given name wins. A plugin module that fails to import because its own underlying library isn't installed is silently skipped (<code>ImportError</code>); any other exception it raises while loading is caught and turned into a <code>RuntimeWarning</code> instead of propagating, so a broken third-party plugin can only fail to register itself, never crash <code>import plotmux</code> for every user.</p> <p><code>api.py</code> never triggers registration itself: it only calls <code>get_backend(name)</code>, which is what actually imports a built-in submodule lazily, or raises <code>BackendNotFoundError</code> if <code>name</code> isn't registered (typically because its library isn't installed). <code>config.set_backend()</code>/<code>backend()</code> additionally reject a name that isn't even known \u2014 neither a built-in name, an entry-point-advertised name, nor already registered \u2014 immediately, at the call site, without importing anything (<code>backends/registry.py::known_backend_names()</code>); a name that is known but not yet registered still only fails later, at render time, since validating that would require the same import laziness was introduced to avoid.</p>"},{"location":"dev/architecture/#key-components","title":"Key Components","text":""},{"location":"dev/architecture/#basespec","title":"<code>BaseSpec</code>","text":"<p>Holds the figure-level fields every chart type inherits (<code>title</code>, <code>xlabel</code>, <code>ylabel</code>, <code>xscale</code>, <code>yscale</code>) so they are defined once instead of being redeclared per chart type, and gives <code>Backend.render</code>/the <code>_RENDERERS</code> dicts a common type to dispatch on. These fields are <code>kw_only=True</code> so they (all defaulted) can precede a subclass's own required fields (e.g. <code>HistogramSpec.values</code>) without violating the dataclass \"no non-default field after a default field\" rule. It also owns two small helpers a concrete spec's own <code>__post_init__</code> calls instead of reimplementing: <code>_normalize_color(name=\"color\")</code> parses a <code>str | tuple | None</code> color field via <code>parse_color</code> and writes the canonical RGBA value back in place (<code>object.__setattr__</code>, since specs are frozen), and the module-level <code>_check_equal_length(x, y)</code> coerces <code>x</code>/<code>y</code> to <code>np.ndarray</code> and raises <code>InvalidSpecError</code> if their lengths differ (shared by <code>LineSpec</code> and <code>ScatterSpec</code>).</p>"},{"location":"dev/architecture/#color-parsing","title":"Color Parsing","text":"<p><code>parse_color()</code> normalizes the color formats users already know (hex strings, CSS/Matplotlib named colors, RGB(A) float tuples) into one canonical representation, an RGBA tuple of floats in <code>[0, 1]</code>. A spec normalizes its own <code>color</code> field in <code>__post_init__</code>, so every backend always receives an already-validated, canonical color. The named-color table is bundled with <code>plotmux</code> itself, so it resolves without Matplotlib installed.</p>"},{"location":"dev/architecture/#cdfspec","title":"<code>CdfSpec</code>","text":"<p><code>CdfSpec</code> plots the empirical cumulative distribution function of <code>values</code>, approximated as a binned step curve. <code>plotmux.utils.cdf.compute_cdf_steps()</code> computes the <code>(x, y)</code> step vertices shared by every backend except matplotlib, which instead calls <code>Axes.hist(..., cumulative=True, histtype=\"step\")</code> directly (see <code>plotmux.backends.matplotlib.cdf</code>). Unlike the other specs, <code>CdfSpec.ylabel</code> defaults to <code>\"cumulative probability\"</code> instead of <code>None</code>.</p>"},{"location":"dev/architecture/#backend-and-the-registry","title":"<code>Backend</code> and the Registry","text":"<p><code>Backend</code> is an ABC with two methods, <code>render(spec, **kwargs)</code> and <code>save(native, path, fmt)</code>, plus a <code>supported_formats: ClassVar[frozenset[str]]</code> each concrete backend declares. <code>resolve_renderer()</code> and <code>check_export_format()</code> in <code>backends/base.py</code> factor out the lookup-or-raise dispatch logic (<code>{spec_type: renderer}</code> lookup, <code>{format}</code> membership check) shared by every backend, so a backend only owns its dict of renderers, not the dispatch mechanics around it. <code>backends/registry.py</code> maps a backend name to its registered instance, and raises a <code>BackendNotFoundError</code> (also a <code>RuntimeError</code>) listing available backends when an unregistered name is requested, typically because its underlying plotting library is not installed.</p>"},{"location":"dev/architecture/#plotmuxexceptions","title":"<code>plotmux.exceptions</code>","text":"<p>Every exception <code>plotmux</code> raises is a <code>PlotmuxError</code>, in addition to whichever standard-library exception type the raise site already used (<code>ValueError</code>, <code>RuntimeError</code>, <code>NotImplementedError</code>). Each concrete exception (<code>InvalidSpecError</code>, <code>InvalidColorError</code>, <code>UnsupportedSpecError</code>, <code>UnsupportedFormatError</code>, <code>ExportError</code>, <code>BackendNotFoundError</code>) multiply-inherits from both, so existing <code>except ValueError</code>-style code keeps working unchanged, while new code can catch anything <code>plotmux</code>-specific in one place with <code>except PlotmuxError</code>.</p>"},{"location":"dev/architecture/#layering-and-grid-layouts","title":"Layering and Grid Layouts","text":"<p><code>LayerSpec</code> draws multiple child specs on one shared set of axes; <code>GridSpec</code> lays out multiple child specs as independent panels, the backend-agnostic equivalent of Matplotlib's <code>pyplot.subplots()</code>. Both are one flat pass over their children: a <code>LayerSpec</code> nested inside <code>layers</code>, or a <code>GridSpec</code> nested inside <code>cells</code>, is rejected in <code>__post_init__</code>, so callers must flatten nesting themselves rather than relying on recursive dispatch. A <code>GridSpec</code> cell may itself be a <code>LayerSpec</code>, since layering and gridding are independent, composable concerns. Each backend's own <code>backends/&lt;name&gt;/layer.py</code> / <code>backends/&lt;name&gt;/grid.py</code> owns composing the children onto shared axes/chart state or independent panels; every built-in backend implements both. <code>GridSpec</code> inherits <code>xlabel</code>/<code>ylabel</code>/<code>xscale</code>/<code>yscale</code> from <code>BaseSpec</code> but every backend's grid renderer ignores them, since each panel keeps its own; <code>grid()</code> in <code>api.py</code> does not even expose them as parameters.</p> <p><code>LayerSpec.__post_init__</code> also assigns successive <code>DEFAULT_PALETTE</code> entries (see Color Parsing) to any child spec whose own <code>color</code> field is left <code>None</code>, skipping children that already set one explicitly. Matplotlib gets distinct per-child colors for free from its own <code>Axes</code> color cycle when children share an axes, but <code>xy</code>/<code>bokeh</code>/<code>altair</code>/<code>plotly</code> do not, so this assignment happens once, backend-agnostically, at the spec layer (<code>specs/layer.py::_assign_default_colors</code>), rather than every backend needing its own workaround. <code>GridSpec</code> gets no such assignment: each cell is visually independent, so there is no shared-axes indistinguishability problem to solve there.</p> <p><code>xy</code>'s grid support is the one asymmetry across backends: <code>xy</code> has no composition primitive for arbitrary, already-built, independent panels (<code>xy.facet_chart</code> is data-driven faceting, not this), so <code>backends/xy/grid.py::render_grid</code> returns a small <code>XyGrid</code> (the per-cell charts, <code>ncols</code>, <code>title</code>) instead of a bare <code>xy.Chart</code>, deferring layout to export time: <code>XyBackend.save</code> embeds each cell's standalone HTML document in its own sandboxed <code>&lt;iframe srcdoc=...&gt;</code> and arranges them with CSS grid, which makes <code>grid(..., backend=\"xy\")</code> <code>\"html\"</code>-only \u2014 every other format raises <code>UnsupportedFormatError</code> for an <code>XyGrid</code>, even though a bare (non-grid) <code>xy.Chart</code> supports the full <code>supported_formats</code> set.</p>"},{"location":"dev/architecture/#figure","title":"<code>Figure</code>","text":"<p>The object returned to the user by every public plotting function. It is a thin wrapper holding <code>spec</code>, <code>backend_name</code>, and <code>native</code>, plus three convenience methods: <code>show()</code>, <code>save(path)</code> (delegating to <code>plotmux.export.save</code>), and <code>to_native()</code>, the escape hatch to backend-specific functionality not exposed by the unified API.</p>"},{"location":"dev/architecture/#config","title":"<code>config</code>","text":"<p>The current default backend is stored in a <code>contextvars.ContextVar</code> rather than a plain module global, so it is thread/task-local: <code>set_backend()</code>/<code>backend(...)</code> in one thread or <code>asyncio</code> task never leaks into or races with another one, while still behaving like a single process-wide default in the common single-threaded case.</p> <p><code>set_backend()</code> also validates its <code>name</code> argument against <code>known_backend_names()</code> before storing it, raising <code>BackendNotFoundError</code> immediately for a name that's neither built-in, advertised by an installed entry-point plugin, nor already registered \u2014 this catches a typo'd backend name at the <code>set_backend</code> call site instead of only on the next plotting call, at zero import cost (see Data Flow); <code>backend(...)</code> inherits the same check since it calls <code>set_backend()</code> internally.</p>"},{"location":"dev/architecture/#constraints-from-the-existing-codebase","title":"Constraints From the Existing Codebase","text":"<ul> <li><code>coola</code>'s optional-dependency pattern (<code>is_*_available</code>, <code>*_available</code>, <code>raise_*_missing_error</code>)   is reused as-is by <code>plotmux.utils.imports</code>. New backends follow the same pattern instead of   introducing a new one.</li> <li><code>matplotlib</code>, <code>xy</code>, <code>bokeh</code>, <code>altair</code>, and <code>plotly</code> are optional extras (<code>xy</code> is further gated to   <code>python_version &gt;= '3.11'</code> in its extra marker). Only <code>numpy</code> and <code>coola</code> are hard dependencies,   so the core package (specs, registry, config, public API) must import cleanly with no plotting   library installed.</li> <li><code>plotmux.utils.range.find_range</code> implements quantile-or-explicit axis bounds (e.g. <code>xmin=\"q0.1\"</code>)   once, and is reused by every spec that needs it rather than reimplemented.</li> <li>Style conventions: <code>from __future__ import annotations</code>, explicit <code>__all__</code>, Google-style   docstrings with runnable <code>pycon</code> examples, <code>ruff</code>/<code>black</code>/<code>pyright</code> clean, tests split into   <code>tests/unit/</code> and <code>tests/integration/</code> mirroring <code>src/plotmux/</code>.</li> </ul>"},{"location":"dev/architecture/#whats-next","title":"What's Next","text":"<ul> <li>Development Guide: setting up the development environment</li> <li>User Guide: the public API this architecture supports</li> </ul>"},{"location":"dev/development/","title":"Development Guide","text":"<p>This guide covers setting up your development environment and common development tasks.</p>"},{"location":"dev/development/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10 or higher</li> <li><code>uv</code> for dependency management</li> <li>Git for version control</li> <li>Basic knowledge of Python and testing</li> </ul>"},{"location":"dev/development/#initial-setup","title":"Initial Setup","text":""},{"location":"dev/development/#1-fork-and-clone","title":"1. Fork and Clone","text":"<pre><code># Fork the repository on GitHub, then clone your fork\ngit clone https://github.com/YOUR-USERNAME/plotmux.git\ncd plotmux\n</code></pre>"},{"location":"dev/development/#2-set-up-virtual-environment","title":"2. Set Up Virtual Environment","text":"<p>The project uses <code>uv</code> for dependency management. First, install <code>uv</code> if you don't have it:</p> <pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh\n</code></pre> <p>Create and set up the virtual environment:</p> <pre><code>make setup-venv\n</code></pre> <p>This will create a virtual environment and install all dependencies, including development tools and documentation dependencies.</p> <p>Activate the virtual environment:</p> <pre><code>source .venv/bin/activate\n</code></pre>"},{"location":"dev/development/#3-install-dependencies","title":"3. Install Dependencies","text":"<p>If you already have a virtual environment and just want to install dependencies:</p> <pre><code># Install core dependencies\ninv install --no-optional-deps\n\n# Install with documentation dependencies\ninv install --docs-deps\n</code></pre>"},{"location":"dev/development/#4-set-up-pre-commit-hooks","title":"4. Set Up Pre-commit Hooks","text":"<pre><code>pre-commit install\n</code></pre> <p>This will automatically run code quality checks before each commit.</p>"},{"location":"dev/development/#development-workflow","title":"Development Workflow","text":""},{"location":"dev/development/#running-tests","title":"Running Tests","text":"<p>Run all unit tests:</p> <pre><code>inv unit-test\n</code></pre> <p>Run unit tests with coverage:</p> <pre><code>inv unit-test --cov\n</code></pre> <p>Run integration tests:</p> <pre><code>inv integration-test\n</code></pre> <p>Run a specific test file:</p> <pre><code>pytest tests/unit/test_api.py\n</code></pre> <p>Run a specific test:</p> <pre><code>pytest tests/unit/test_api.py::test_hist\n</code></pre>"},{"location":"dev/development/#code-quality","title":"Code Quality","text":"<p>Format code with Black:</p> <pre><code>inv check-format\n</code></pre> <p>Run the linter (Ruff):</p> <pre><code>inv check-lint\n</code></pre> <p>Format docstrings:</p> <pre><code>inv docformat\n</code></pre> <p>Run all pre-commit checks:</p> <pre><code>pre-commit run --all-files\n</code></pre>"},{"location":"dev/development/#documentation","title":"Documentation","text":"<p>Build documentation locally:</p> <pre><code>mkdocs serve -f docs/mkdocs.yml\n</code></pre> <p>Then open http://127.0.0.1:8000 in your browser.</p> <p>Build documentation without serving:</p> <pre><code>mkdocs build -f docs/mkdocs.yml\n</code></pre> <p>Run doctests:</p> <pre><code>inv doctest-src\n</code></pre>"},{"location":"dev/development/#type-checking","title":"Type Checking","text":"<p><code>plotmux</code> uses pyright for type checking. You can run type checking locally:</p> <pre><code>inv check-types\n</code></pre>"},{"location":"dev/development/#project-structure","title":"Project Structure","text":"<pre><code>plotmux/\n\u251c\u2500\u2500 .github/                    # GitHub configuration\n\u2502   \u251c\u2500\u2500 workflows/              # CI/CD workflows\n\u2502   \u2514\u2500\u2500 ISSUE_TEMPLATE/         # Issue templates\n\u251c\u2500\u2500 docs/                       # Documentation\n\u2502   \u251c\u2500\u2500 docs/                   # Documentation source\n\u2502   \u2514\u2500\u2500 mkdocs.yml              # MkDocs configuration\n\u251c\u2500\u2500 src/plotmux/\n\u2502   \u251c\u2500\u2500 api.py                  # Public plotting API (hist, cdf, line, scatter, layer, grid)\n\u2502   \u251c\u2500\u2500 config.py               # Default-backend configuration\n\u2502   \u251c\u2500\u2500 figure.py                # Figure wrapper\n\u2502   \u251c\u2500\u2500 export.py                # Figure export utilities\n\u2502   \u251c\u2500\u2500 exceptions.py            # PlotmuxError hierarchy\n\u2502   \u251c\u2500\u2500 specs/                  # Backend-agnostic chart specifications\n\u2502   \u251c\u2500\u2500 backends/                # One package per rendering backend\n\u2502   \u2502   \u251c\u2500\u2500 base.py             # Backend interface + dispatch helpers\n\u2502   \u2502   \u251c\u2500\u2500 registry.py         # Backend registration/lookup\n\u2502   \u2502   \u251c\u2500\u2500 matplotlib/         # Matplotlib backend\n\u2502   \u2502   \u251c\u2500\u2500 xy/                 # xy backend\n\u2502   \u2502   \u251c\u2500\u2500 bokeh/               # Bokeh backend\n\u2502   \u2502   \u2514\u2500\u2500 altair/              # Altair backend\n\u2502   \u251c\u2500\u2500 colors/                  # Color parsing and palettes\n\u2502   \u251c\u2500\u2500 testing/                 # pytest fixtures for downstream users\n\u2502   \u2514\u2500\u2500 utils/                   # Small utilities (ranges, optional imports)\n\u251c\u2500\u2500 tests/                      # Test files\n\u2502   \u251c\u2500\u2500 unit/                   # Unit tests\n\u2502   \u2514\u2500\u2500 integration/            # Integration tests\n\u251c\u2500\u2500 pyproject.toml               # Project configuration\n\u251c\u2500\u2500 uv.lock                      # Locked dependencies\n\u251c\u2500\u2500 LICENSE                      # License file\n\u251c\u2500\u2500 README.md                    # Project README\n\u2514\u2500\u2500 DESIGN.md                    # Design document\n</code></pre>"},{"location":"dev/development/#common-development-tasks","title":"Common Development Tasks","text":""},{"location":"dev/development/#adding-a-new-chart-type","title":"Adding a New Chart Type","text":"<ol> <li> <p>Create a new branch: <pre><code>git checkout -b feature/my-chart-type\n</code></pre></p> </li> <li> <p>Add a spec in <code>src/plotmux/specs/</code>, subclassing <code>BaseSpec</code> as a frozen dataclass. Specs    never import a plotting library.</p> </li> <li> <p>Add a renderer for each backend (<code>src/plotmux/backends/matplotlib/</code>,    <code>src/plotmux/backends/xy/</code>, <code>src/plotmux/backends/bokeh/</code>, <code>src/plotmux/backends/altair/</code>,    <code>src/plotmux/backends/plotly/</code>), and    register it in that backend's <code>_RENDERERS</code> dict.</p> </li> <li> <p>Export the spec and the public function from <code>src/plotmux/specs/__init__.py</code>,    <code>src/plotmux/api.py</code>, and <code>src/plotmux/__init__.py</code>.</p> </li> <li> <p>Add tests in <code>tests/unit/</code> and <code>tests/integration/</code>, mirroring <code>src/plotmux/</code>.</p> </li> <li> <p>Update documentation in <code>docs/docs/</code>.</p> </li> <li> <p>Run tests and code quality checks: <pre><code>inv unit-test --cov\npre-commit run --all-files\n</code></pre></p> </li> <li> <p>Commit and push: <pre><code>git add .\ngit commit -m \"Add: brief description of the new chart type\"\ngit push origin feature/my-chart-type\n</code></pre></p> </li> </ol>"},{"location":"dev/development/#adding-a-new-backend","title":"Adding a New Backend","text":"<p>See Adding a Third-Party Backend for the plugin mechanism used by out-of-tree backends, and <code>src/plotmux/backends/matplotlib/</code> or <code>src/plotmux/backends/xy/</code> as a reference implementation for a built-in one.</p>"},{"location":"dev/development/#fixing-a-bug","title":"Fixing a Bug","text":"<ol> <li> <p>Create a branch: <pre><code>git checkout -b fix/bug-description\n</code></pre></p> </li> <li> <p>Write a failing test that reproduces the bug</p> </li> <li> <p>Fix the bug</p> </li> <li> <p>Verify the test passes: <pre><code>pytest tests/unit/path/to/test.py\n</code></pre></p> </li> <li> <p>Run the full test suite: <pre><code>inv unit-test --cov\n</code></pre></p> </li> <li> <p>Commit and push: <pre><code>git commit -m \"Fix: description of bug fix\"\ngit push origin fix/bug-description\n</code></pre></p> </li> </ol>"},{"location":"dev/development/#updating-dependencies","title":"Updating Dependencies","text":"<pre><code>inv update\n</code></pre> <p>Dependencies are managed in <code>pyproject.toml</code> and locked in <code>uv.lock</code>.</p>"},{"location":"dev/development/#testing-guidelines","title":"Testing Guidelines","text":""},{"location":"dev/development/#writing-good-tests","title":"Writing Good Tests","text":"<ol> <li> <p>Use descriptive names: <pre><code>def test_hist_returns_figure(): ...\n\n\ndef test_hist_raises_error_for_non_positive_bins(): ...\n</code></pre></p> </li> <li> <p>Test edge cases:</p> <ul> <li>Empty arrays</li> <li><code>None</code> values</li> <li>Invalid colors or ranges</li> <li>Every combination of chart type x backend x export format</li> </ul> </li> <li> <p>Use <code>plotmux.testing</code> fixtures to skip tests based on installed backends: <pre><code>from plotmux.testing import matplotlib_available\n\n\n@matplotlib_available\ndef test_hist_with_matplotlib_backend():\n    fig = plotmux.hist([1, 2, 3], backend=\"matplotlib\")\n    assert fig.backend_name == \"matplotlib\"\n</code></pre></p> </li> <li> <p>Test both success and failure cases: <pre><code>def test_success_case():\n    assert plotmux.hist([1, 2, 3]) is not None\n\n\ndef test_failure_case():\n    with pytest.raises(ValueError, match=\"bins must be a positive integer\"):\n        HistogramSpec(values=np.arange(10), bins=0)\n</code></pre></p> </li> </ol>"},{"location":"dev/development/#continuous-integration","title":"Continuous Integration","text":"<p>The project uses GitHub Actions for CI. Workflows are in <code>.github/workflows/</code>:</p> <ul> <li> <p>CI: Runs on every push and PR</p> <ul> <li>Linting</li> <li>Tests</li> <li>Coverage</li> </ul> </li> <li> <p>Documentation: Builds and deploys docs</p> <ul> <li>Builds on every push</li> <li>Deploys on release</li> </ul> </li> <li> <p>Nightly Tests: Tests against latest dependencies</p> <ul> <li>Runs daily</li> <li>Tests multiple Python versions</li> </ul> </li> </ul>"},{"location":"dev/development/#best-practices","title":"Best Practices","text":"<ol> <li>Write tests first (TDD approach when possible)</li> <li>Keep PRs focused on a single feature or fix</li> <li>Update documentation for user-facing changes</li> <li>Run pre-commit hooks before committing</li> <li>Write clear commit messages</li> <li>Add docstrings, with a runnable <code>pycon</code> example, to all public APIs</li> <li>Keep dependencies minimal: new hard dependencies need a strong justification; prefer an    optional extra</li> <li>Follow existing code style, in particular the separation between specs (backend-agnostic)    and backends (library-specific), see Architecture</li> </ol>"},{"location":"dev/development/#code-review-checklist","title":"Code Review Checklist","text":"<p>Before submitting a PR, ensure:</p> <ul> <li>[ ] All tests pass locally</li> <li>[ ] Code coverage is maintained or improved</li> <li>[ ] Pre-commit hooks pass</li> <li>[ ] Documentation is updated</li> <li>[ ] Commit messages are clear</li> <li>[ ] Code follows project style</li> <li>[ ] No unnecessary dependencies added</li> <li>[ ] Examples are provided for new features</li> <li>[ ] Edge cases are tested</li> </ul>"},{"location":"dev/development/#additional-resources","title":"Additional Resources","text":"<ul> <li>Python Packaging Guide</li> <li>pytest Documentation</li> <li>uv Documentation</li> <li>MkDocs Documentation</li> <li>GitHub Actions Documentation</li> </ul>"},{"location":"refs/api/","title":"Api","text":""},{"location":"refs/api/#plotmux.api","title":"plotmux.api","text":"<p>Contain the public plotting API.</p>"},{"location":"refs/api/#plotmux.api.bar","title":"plotmux.api.bar","text":"<pre><code>bar(\n    x: Sequence[float] | ndarray,\n    y: Sequence[float] | ndarray,\n    *,\n    label: str | None = None,\n    color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    width: float = 0.8,\n    alpha: float | None = None,\n    title: str | None = None,\n    xlabel: str | None = None,\n    ylabel: str | None = None,\n    xscale: Literal[\"linear\", \"log\"] = \"linear\",\n    yscale: Literal[\"linear\", \"log\"] = \"linear\",\n    background_color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    ymin: float | None = None,\n    ymax: float | None = None,\n    backend: str | None = None,\n    **kwargs: Any\n) -&gt; Figure\n</code></pre> <p>Plot a bar chart.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Sequence[float] | ndarray</code> <p>The array of bar positions.</p> required <code>y</code> <code>Sequence[float] | ndarray</code> <p>The array of bar heights. Must have the same length as <code>x</code>.</p> required <code>label</code> <code>str | None</code> <p>An optional label used e.g. in the legend.</p> <code>None</code> <code>color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional color for the bars. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default color.</p> <code>None</code> <code>width</code> <code>float</code> <p>The width of each bar, in <code>x</code> data units. Must be a positive number.</p> <code>0.8</code> <code>alpha</code> <code>float | None</code> <p>An optional bar opacity, in <code>[0, 1]</code>. <code>None</code> uses the backend's default (usually fully opaque).</p> <code>None</code> <code>title</code> <code>str | None</code> <p>An optional figure title.</p> <code>None</code> <code>xlabel</code> <code>str | None</code> <p>An optional x-axis label.</p> <code>None</code> <code>ylabel</code> <code>str | None</code> <p>An optional y-axis label.</p> <code>None</code> <code>xscale</code> <code>Literal['linear', 'log']</code> <p>The x-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>yscale</code> <code>Literal['linear', 'log']</code> <p>The y-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>background_color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional figure background color. Same format as <code>color</code>. <code>None</code> uses the backend's default.</p> <code>None</code> <code>ymin</code> <code>float | None</code> <p>An optional explicit lower bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>ymax</code> <code>float | None</code> <p>An optional explicit upper bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>backend</code> <code>str | None</code> <p>The name of the backend to use to render the figure, or <code>None</code> to use the current default backend (see <code>plotmux.set_backend</code>).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional backend-specific keyword arguments, forwarded to the backend's renderer.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Figure</code> <p>The rendered figure.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>x</code> and <code>y</code> do not have the same length, <code>width</code> is not a positive number, <code>alpha</code> is not in <code>[0, 1]</code>, <code>color</code>/<code>background_color</code> is not a valid color, or <code>ymin</code>/<code>ymax</code> are both set with <code>ymin &gt; ymax</code>.</p> Example <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.bar([1, 2, 3], [4, 5, 6])  # doctest: +SKIP\n</code></pre>"},{"location":"refs/api/#plotmux.api.cdf","title":"plotmux.api.cdf","text":"<pre><code>cdf(\n    values: Sequence[float] | ndarray,\n    *,\n    nbins: int | None = None,\n    xmin: float | str | None = None,\n    xmax: float | str | None = None,\n    label: str | None = None,\n    color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    alpha: float | None = None,\n    title: str | None = None,\n    xlabel: str | None = None,\n    ylabel: str | None = \"cumulative probability\",\n    xscale: Literal[\"linear\", \"log\"] = \"linear\",\n    yscale: Literal[\"linear\", \"log\"] = \"linear\",\n    background_color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    ymin: float | None = None,\n    ymax: float | None = None,\n    backend: str | None = None,\n    **kwargs: Any\n) -&gt; Figure\n</code></pre> <p>Plot the empirical cumulative distribution function (CDF) of an array of values.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>Sequence[float] | ndarray</code> <p>The array of values to plot.</p> required <code>nbins</code> <code>int | None</code> <p>The number of bins to use to approximate the cumulative distribution. Must be a positive integer, or <code>None</code> to use the backend's default binning.</p> <code>None</code> <code>xmin</code> <code>float | str | None</code> <p>Specifies the lower bound of the x-axis range. It can be an explicit value, a quantile string such as <code>\"q0.1\"</code>, or <code>None</code> to use the minimum of <code>values</code>.</p> <code>None</code> <code>xmax</code> <code>float | str | None</code> <p>Specifies the upper bound of the x-axis range. Same semantics as <code>xmin</code> but for the upper bound.</p> <code>None</code> <code>label</code> <code>str | None</code> <p>An optional label used e.g. in the legend.</p> <code>None</code> <code>color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional color for the curve. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default color.</p> <code>None</code> <code>alpha</code> <code>float | None</code> <p>An optional curve opacity, in <code>[0, 1]</code>. <code>None</code> uses the backend's default (usually fully opaque).</p> <code>None</code> <code>title</code> <code>str | None</code> <p>An optional figure title.</p> <code>None</code> <code>xlabel</code> <code>str | None</code> <p>An optional x-axis label.</p> <code>None</code> <code>ylabel</code> <code>str | None</code> <p>An optional y-axis label. Defaults to <code>\"cumulative probability\"</code>.</p> <code>'cumulative probability'</code> <code>xscale</code> <code>Literal['linear', 'log']</code> <p>The x-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>yscale</code> <code>Literal['linear', 'log']</code> <p>The y-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>background_color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional figure background color. Same format as <code>color</code>. <code>None</code> uses the backend's default.</p> <code>None</code> <code>ymin</code> <code>float | None</code> <p>An optional explicit lower bound for the y-axis. <code>None</code> leaves the axis autoscaled (bokeh's <code>CdfSpec</code> renderer hardcodes its own <code>0</code>/<code>1</code> bounds regardless; an explicit <code>ymin</code>/<code>ymax</code> here overrides that, see <code>plotmux.backends.bokeh.cdf.render_cdf</code>).</p> <code>None</code> <code>ymax</code> <code>float | None</code> <p>An optional explicit upper bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>backend</code> <code>str | None</code> <p>The name of the backend to use to render the figure, or <code>None</code> to use the current default backend (see <code>plotmux.set_backend</code>).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional backend-specific keyword arguments, forwarded to the backend's renderer.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Figure</code> <p>The rendered figure.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>nbins</code> is set and is not a positive integer, <code>alpha</code> is not in <code>[0, 1]</code>, <code>color</code>/<code>background_color</code> is not a valid color, <code>xmin</code> and <code>xmax</code> are both explicit numeric values with <code>xmin &gt;= xmax</code>, or <code>ymin</code>/<code>ymax</code> are both set with <code>ymin &gt; ymax</code>.</p> Example <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.cdf([1, 2, 2, 3, 3, 3])  # doctest: +SKIP\n</code></pre>"},{"location":"refs/api/#plotmux.api.grid","title":"plotmux.api.grid","text":"<pre><code>grid(\n    *items: BaseSpec | Figure,\n    ncols: int = 1,\n    title: str | None = None,\n    backend: str | None = None,\n    **kwargs: Any\n) -&gt; Figure\n</code></pre> <p>Lay out specs (or already-rendered <code>Figure</code>s) as independent panels in a grid.</p> <p>Unlike <code>layer</code> (which draws every child onto one shared axes), each item here gets its own, independent panel -- the backend-agnostic equivalent of matplotlib's <code>pyplot.subplots</code>. An item may itself be built with <code>layer(...)</code> (several series sharing one panel), since layering and gridding are independent, composable concerns.</p> <p>Parameters:</p> Name Type Description Default <code>*items</code> <code>BaseSpec | Figure</code> <p>The child specs to draw, one independent panel per item, in row-major order (left to right, top to bottom). A <code>Figure</code> (e.g. one returned by <code>plotmux.line(...)</code>) is accepted as shorthand for its <code>.spec</code>, same as <code>layer</code>: only the spec is reused, the earlier native figure is discarded and everything is re-rendered together.</p> <code>()</code> <code>ncols</code> <code>int</code> <p>The number of columns in the grid. Rows are filled left to right; the last row is left short when <code>len(items)</code> is not a multiple of <code>ncols</code>. Must be a positive integer.</p> <code>1</code> <code>title</code> <code>str | None</code> <p>An optional figure-level title, shown once above the whole grid (not any individual panel).</p> <code>None</code> <code>backend</code> <code>str | None</code> <p>The name of the backend to use to render the figure, or <code>None</code> to use the current default backend (see <code>plotmux.set_backend</code>).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional backend-specific keyword arguments, forwarded to every panel's renderer call.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Figure</code> <p>The rendered figure.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if no <code>items</code> are given, one of the given specs is itself a <code>GridSpec</code> (nesting is not supported), or <code>ncols</code> is not a positive integer.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; from plotmux.specs import HistogramSpec\n&gt;&gt;&gt; fig = plotmux.grid(\n...     HistogramSpec(values=np.arange(101), bins=10),\n...     plotmux.line([0, 100], [0, 5]),\n...     ncols=2,\n... )  # doctest: +SKIP\n</code></pre>"},{"location":"refs/api/#plotmux.api.hist","title":"plotmux.api.hist","text":"<pre><code>hist(\n    values: Sequence[float] | ndarray,\n    *,\n    bins: int = 30,\n    xmin: float | str | None = None,\n    xmax: float | str | None = None,\n    label: str | None = None,\n    density: bool = False,\n    color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    alpha: float | None = None,\n    title: str | None = None,\n    xlabel: str | None = None,\n    ylabel: str | None = None,\n    xscale: Literal[\"linear\", \"log\"] = \"linear\",\n    yscale: Literal[\"linear\", \"log\"] = \"linear\",\n    background_color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    ymin: float | None = None,\n    ymax: float | None = None,\n    backend: str | None = None,\n    **kwargs: Any\n) -&gt; Figure\n</code></pre> <p>Plot a histogram.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>Sequence[float] | ndarray</code> <p>The array of values to plot.</p> required <code>bins</code> <code>int</code> <p>The number of histogram bins. Must be a positive integer.</p> <code>30</code> <code>xmin</code> <code>float | str | None</code> <p>Specifies the lower bound of the x-axis range. It can be an explicit value, a quantile string such as <code>\"q0.1\"</code>, or <code>None</code> to use the minimum of <code>values</code>.</p> <code>None</code> <code>xmax</code> <code>float | str | None</code> <p>Specifies the upper bound of the x-axis range. Same semantics as <code>xmin</code> but for the upper bound.</p> <code>None</code> <code>label</code> <code>str | None</code> <p>An optional label used e.g. in the legend.</p> <code>None</code> <code>density</code> <code>bool</code> <p>If <code>True</code>, draw and return a probability density: each bin will display the bin's raw count divided by the total number of counts and the bin width, so that the area under the histogram integrates to 1. Defaults to <code>False</code>.</p> <code>False</code> <code>color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional color for the bars. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default color.</p> <code>None</code> <code>alpha</code> <code>float | None</code> <p>An optional bar opacity, in <code>[0, 1]</code>. <code>None</code> uses the backend's default (usually fully opaque).</p> <code>None</code> <code>title</code> <code>str | None</code> <p>An optional figure title.</p> <code>None</code> <code>xlabel</code> <code>str | None</code> <p>An optional x-axis label.</p> <code>None</code> <code>ylabel</code> <code>str | None</code> <p>An optional y-axis label.</p> <code>None</code> <code>xscale</code> <code>Literal['linear', 'log']</code> <p>The x-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>yscale</code> <code>Literal['linear', 'log']</code> <p>The y-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>background_color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional figure background color. Same format as <code>color</code>. <code>None</code> uses the backend's default.</p> <code>None</code> <code>ymin</code> <code>float | None</code> <p>An optional explicit lower bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>ymax</code> <code>float | None</code> <p>An optional explicit upper bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>backend</code> <code>str | None</code> <p>The name of the backend to use to render the figure, or <code>None</code> to use the current default backend (see <code>plotmux.set_backend</code>).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional backend-specific keyword arguments, forwarded to the backend's renderer.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Figure</code> <p>The rendered figure.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>bins</code> is not a positive integer, <code>alpha</code> is not in <code>[0, 1]</code>, <code>color</code>/<code>background_color</code> is not a valid color, <code>xmin</code> and <code>xmax</code> are both explicit numeric values with <code>xmin &gt;= xmax</code>, or <code>ymin</code>/<code>ymax</code> are both set with <code>ymin &gt; ymax</code>.</p> Example <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 2, 3, 3, 3], bins=3)  # doctest: +SKIP\n</code></pre>"},{"location":"refs/api/#plotmux.api.layer","title":"plotmux.api.layer","text":"<pre><code>layer(\n    *items: BaseSpec | Figure,\n    title: str | None = None,\n    xlabel: str | None = None,\n    ylabel: str | None = None,\n    xscale: Literal[\"linear\", \"log\"] = \"linear\",\n    yscale: Literal[\"linear\", \"log\"] = \"linear\",\n    background_color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    ymin: float | None = None,\n    ymax: float | None = None,\n    backend: str | None = None,\n    **kwargs: Any\n) -&gt; Figure\n</code></pre> <p>Combine specs (or already-rendered <code>Figure</code>s) onto one shared axes.</p> <p>Parameters:</p> Name Type Description Default <code>*items</code> <code>BaseSpec | Figure</code> <p>The child specs to draw together, in draw order. A <code>Figure</code> (e.g. one returned by <code>plotmux.line(...)</code>) is accepted as shorthand for its <code>.spec</code>: only the spec is reused, the earlier native figure is discarded and everything is re-rendered together, since two independent native figures can't be merged after the fact in either backend.</p> <code>()</code> <code>title</code> <code>str | None</code> <p>An optional figure title, describing the combined axes (not any individual child).</p> <code>None</code> <code>xlabel</code> <code>str | None</code> <p>An optional x-axis label.</p> <code>None</code> <code>ylabel</code> <code>str | None</code> <p>An optional y-axis label.</p> <code>None</code> <code>xscale</code> <code>Literal['linear', 'log']</code> <p>The x-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>yscale</code> <code>Literal['linear', 'log']</code> <p>The y-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>background_color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional figure background color. It can be a hex string, a CSS/matplotlib named color, or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default.</p> <code>None</code> <code>ymin</code> <code>float | None</code> <p>An optional explicit lower bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>ymax</code> <code>float | None</code> <p>An optional explicit upper bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>backend</code> <code>str | None</code> <p>The name of the backend to use to render the figure, or <code>None</code> to use the current default backend (see <code>plotmux.set_backend</code>).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional backend-specific keyword arguments, forwarded to every child's renderer call.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Figure</code> <p>The rendered figure.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if no <code>items</code> are given, one of the given specs is itself a <code>LayerSpec</code> (nesting is not supported), <code>background_color</code> is not a valid color, or <code>ymin</code>/<code>ymax</code> are both set with <code>ymin &gt; ymax</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; from plotmux.specs import HistogramSpec\n&gt;&gt;&gt; fig = plotmux.layer(\n...     HistogramSpec(values=np.arange(101), bins=10),\n...     plotmux.line([0, 100], [0, 5]),\n... )  # doctest: +SKIP\n</code></pre>"},{"location":"refs/api/#plotmux.api.line","title":"plotmux.api.line","text":"<pre><code>line(\n    x: Sequence[float] | ndarray,\n    y: Sequence[float] | ndarray,\n    *,\n    label: str | None = None,\n    color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    alpha: float | None = None,\n    linewidth: float | None = None,\n    linestyle: Literal[\n        \"solid\", \"dashed\", \"dotted\", \"dashdot\"\n    ] = \"solid\",\n    title: str | None = None,\n    xlabel: str | None = None,\n    ylabel: str | None = None,\n    xscale: Literal[\"linear\", \"log\"] = \"linear\",\n    yscale: Literal[\"linear\", \"log\"] = \"linear\",\n    background_color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    ymin: float | None = None,\n    ymax: float | None = None,\n    backend: str | None = None,\n    **kwargs: Any\n) -&gt; Figure\n</code></pre> <p>Plot a line chart.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Sequence[float] | ndarray</code> <p>The array of x values.</p> required <code>y</code> <code>Sequence[float] | ndarray</code> <p>The array of y values. Must have the same length as <code>x</code>.</p> required <code>label</code> <code>str | None</code> <p>An optional label used e.g. in the legend.</p> <code>None</code> <code>color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional color for the line. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default color.</p> <code>None</code> <code>alpha</code> <code>float | None</code> <p>An optional line opacity, in <code>[0, 1]</code>. <code>None</code> uses the backend's default (usually fully opaque).</p> <code>None</code> <code>linewidth</code> <code>float | None</code> <p>An optional line width. <code>None</code> uses the backend's default width.</p> <code>None</code> <code>linestyle</code> <code>Literal['solid', 'dashed', 'dotted', 'dashdot']</code> <p>The line's dash style.</p> <code>'solid'</code> <code>title</code> <code>str | None</code> <p>An optional figure title.</p> <code>None</code> <code>xlabel</code> <code>str | None</code> <p>An optional x-axis label.</p> <code>None</code> <code>ylabel</code> <code>str | None</code> <p>An optional y-axis label.</p> <code>None</code> <code>xscale</code> <code>Literal['linear', 'log']</code> <p>The x-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>yscale</code> <code>Literal['linear', 'log']</code> <p>The y-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>background_color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional figure background color. Same format as <code>color</code>. <code>None</code> uses the backend's default.</p> <code>None</code> <code>ymin</code> <code>float | None</code> <p>An optional explicit lower bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>ymax</code> <code>float | None</code> <p>An optional explicit upper bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>backend</code> <code>str | None</code> <p>The name of the backend to use to render the figure, or <code>None</code> to use the current default backend (see <code>plotmux.set_backend</code>).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional backend-specific keyword arguments, forwarded to the backend's renderer.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Figure</code> <p>The rendered figure.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>x</code> and <code>y</code> do not have the same length, <code>alpha</code> is not in <code>[0, 1]</code>, <code>color</code>/<code>background_color</code> is not a valid color, or <code>ymin</code>/<code>ymax</code> are both set with <code>ymin &gt; ymax</code>.</p> Example <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.line([1, 2, 3], [1, 4, 9])  # doctest: +SKIP\n</code></pre>"},{"location":"refs/api/#plotmux.api.scatter","title":"plotmux.api.scatter","text":"<pre><code>scatter(\n    x: Sequence[float] | ndarray,\n    y: Sequence[float] | ndarray,\n    *,\n    label: str | None = None,\n    color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    size: float | None = None,\n    edgecolor: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    alpha: float | None = None,\n    title: str | None = None,\n    xlabel: str | None = None,\n    ylabel: str | None = None,\n    xscale: Literal[\"linear\", \"log\"] = \"linear\",\n    yscale: Literal[\"linear\", \"log\"] = \"linear\",\n    background_color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    ymin: float | None = None,\n    ymax: float | None = None,\n    backend: str | None = None,\n    **kwargs: Any\n) -&gt; Figure\n</code></pre> <p>Plot a scatter chart.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Sequence[float] | ndarray</code> <p>The array of x values.</p> required <code>y</code> <code>Sequence[float] | ndarray</code> <p>The array of y values. Must have the same length as <code>x</code>.</p> required <code>label</code> <code>str | None</code> <p>An optional label used e.g. in the legend.</p> <code>None</code> <code>color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional color for the markers. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default color.</p> <code>None</code> <code>size</code> <code>float | None</code> <p>An optional marker size. <code>None</code> uses the backend's default size.</p> <code>None</code> <code>edgecolor</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional, separate color for the marker edge. Same format as <code>color</code>. <code>None</code> uses <code>color</code> for the edge too.</p> <code>None</code> <code>alpha</code> <code>float | None</code> <p>An optional marker opacity, in <code>[0, 1]</code>. <code>None</code> uses the backend's default (usually fully opaque).</p> <code>None</code> <code>title</code> <code>str | None</code> <p>An optional figure title.</p> <code>None</code> <code>xlabel</code> <code>str | None</code> <p>An optional x-axis label.</p> <code>None</code> <code>ylabel</code> <code>str | None</code> <p>An optional y-axis label.</p> <code>None</code> <code>xscale</code> <code>Literal['linear', 'log']</code> <p>The x-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>yscale</code> <code>Literal['linear', 'log']</code> <p>The y-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>background_color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional figure background color. Same format as <code>color</code>. <code>None</code> uses the backend's default.</p> <code>None</code> <code>ymin</code> <code>float | None</code> <p>An optional explicit lower bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>ymax</code> <code>float | None</code> <p>An optional explicit upper bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>backend</code> <code>str | None</code> <p>The name of the backend to use to render the figure, or <code>None</code> to use the current default backend (see <code>plotmux.set_backend</code>).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional backend-specific keyword arguments, forwarded to the backend's renderer.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Figure</code> <p>The rendered figure.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>x</code> and <code>y</code> do not have the same length, <code>size</code> is not a positive number, <code>alpha</code> is not in <code>[0, 1]</code>, <code>color</code>/<code>edgecolor</code>/<code>background_color</code> is not a valid color, or <code>ymin</code>/<code>ymax</code> are both set with <code>ymin &gt; ymax</code>.</p> Example <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.scatter([1, 2, 3], [1, 4, 9])  # doctest: +SKIP\n</code></pre>"},{"location":"refs/api/#plotmux.api.slope","title":"plotmux.api.slope","text":"<pre><code>slope(\n    gradient: float,\n    intercept: float = 0.0,\n    *,\n    label: str | None = None,\n    color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    linewidth: float | None = None,\n    linestyle: Literal[\n        \"solid\", \"dashed\", \"dotted\", \"dashdot\"\n    ] = \"solid\",\n    alpha: float | None = None,\n    title: str | None = None,\n    xlabel: str | None = None,\n    ylabel: str | None = None,\n    xscale: Literal[\"linear\", \"log\"] = \"linear\",\n    yscale: Literal[\"linear\", \"log\"] = \"linear\",\n    background_color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n        | None\n    ) = None,\n    ymin: float | None = None,\n    ymax: float | None = None,\n    backend: str | None = None,\n    **kwargs: Any\n) -&gt; Figure\n</code></pre> <p>Plot a slope (a.k.a. abline) annotation: a line defined by <code>y = gradient * x + intercept</code>, spanning the current axes.</p> <p>Unlike <code>line</code>, this draws no data of its own -- it is a reference/trend line. Not supported by every backend: matplotlib (via <code>Axes.axline</code>) and bokeh (via <code>bokeh.models.Slope</code>) both have a native \"line by slope, independent of data range\" primitive; altair and xy do not (see <code>DESIGN.md</code>, section 8.1), so <code>backend=\"altair\"</code>/<code>backend=\"xy\"</code> raises <code>UnsupportedSpecError</code>. It typically appears as a <code>layer()</code> child alongside a data-bound spec, e.g. a scatter plot with a fitted trend line overlaid.</p> <p>Parameters:</p> Name Type Description Default <code>gradient</code> <code>float</code> <p>The line's slope.</p> required <code>intercept</code> <code>float</code> <p>The line's y-intercept (the <code>y</code> value at <code>x = 0</code>). Defaults to <code>0.0</code>.</p> <code>0.0</code> <code>label</code> <code>str | None</code> <p>An optional label used e.g. in the legend.</p> <code>None</code> <code>color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional color for the line. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default color.</p> <code>None</code> <code>linewidth</code> <code>float | None</code> <p>An optional line width. <code>None</code> uses the backend's default width.</p> <code>None</code> <code>linestyle</code> <code>Literal['solid', 'dashed', 'dotted', 'dashdot']</code> <p>The line's dash style.</p> <code>'solid'</code> <code>alpha</code> <code>float | None</code> <p>An optional line opacity, in <code>[0, 1]</code>. <code>None</code> uses the backend's default (usually fully opaque).</p> <code>None</code> <code>title</code> <code>str | None</code> <p>An optional figure title.</p> <code>None</code> <code>xlabel</code> <code>str | None</code> <p>An optional x-axis label.</p> <code>None</code> <code>ylabel</code> <code>str | None</code> <p>An optional y-axis label.</p> <code>None</code> <code>xscale</code> <code>Literal['linear', 'log']</code> <p>The x-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>yscale</code> <code>Literal['linear', 'log']</code> <p>The y-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>background_color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional figure background color. Same format as <code>color</code>. <code>None</code> uses the backend's default.</p> <code>None</code> <code>ymin</code> <code>float | None</code> <p>An optional explicit lower bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>ymax</code> <code>float | None</code> <p>An optional explicit upper bound for the y-axis. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>backend</code> <code>str | None</code> <p>The name of the backend to use to render the figure, or <code>None</code> to use the current default backend (see <code>plotmux.set_backend</code>).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional backend-specific keyword arguments, forwarded to the backend's renderer.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Figure</code> <p>The rendered figure.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>alpha</code> is not in <code>[0, 1]</code>, <code>color</code>/<code>background_color</code> is not a valid color, or <code>ymin</code>/<code>ymax</code> are both set with <code>ymin &gt; ymax</code>.</p> <code>NotImplementedError</code> <p>if the resolved backend is <code>altair</code> or <code>xy</code> and this <code>slope(...)</code> call is not layered with a data-bound spec (see <code>layer</code>): standalone <code>SlopeSpec</code> support remains matplotlib/bokeh only (see DESIGN.md, section 8.1).</p> Example <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.slope(2, 10, backend=\"matplotlib\")  # doctest: +SKIP\n</code></pre>"},{"location":"refs/backends/","title":"Backends","text":""},{"location":"refs/backends/#plotmux.backends.base","title":"plotmux.backends.base","text":"<p>Contain the <code>Backend</code> base class implemented by rendering backends, plus the small dispatch helpers every backend shares.</p> <p>Each backend (and each backend's layer renderer) dispatches on <code>type(spec)</code> against a <code>{type: renderer}</code> dict. <code>resolve_renderer</code> and <code>check_export_format</code> factor out that repeated lookup-or-raise logic so a backend only owns its dict of renderers, not the dispatch mechanics around it.</p>"},{"location":"refs/backends/#plotmux.backends.base.Backend","title":"plotmux.backends.base.Backend","text":"<p>               Bases: <code>ABC</code></p> <p>Define the interface implemented by a rendering backend.</p> <p>A backend turns a backend-agnostic <code>BaseSpec</code> into its native figure object (e.g. a matplotlib <code>Figure</code>) and knows how to export that native object to a file.</p>"},{"location":"refs/backends/#plotmux.backends.base.Backend.render","title":"plotmux.backends.base.Backend.render","text":"<pre><code>render(spec: BaseSpec, **kwargs: Any) -&gt; Any\n</code></pre> <p>Render a spec into the backend's native figure object.</p> <p>Parameters:</p> Name Type Description Default <code>spec</code> <code>BaseSpec</code> <p>The backend-agnostic spec to render.</p> required <code>**kwargs</code> <code>Any</code> <p>Additional backend-specific keyword arguments, forwarded to the underlying plotting call.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Any</code> <p>The backend's native figure object.</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>if there is no renderer registered for the type of <code>spec</code> in this backend's <code>_RENDERERS</code>.</p>"},{"location":"refs/backends/#plotmux.backends.base.Backend.save","title":"plotmux.backends.base.Backend.save  <code>abstractmethod</code>","text":"<pre><code>save(native: Any, path: Path, fmt: str) -&gt; None\n</code></pre> <p>Export a native figure object to a file.</p> <p>Parameters:</p> Name Type Description Default <code>native</code> <code>Any</code> <p>The native figure object, as returned by <code>render</code>.</p> required <code>path</code> <code>Path</code> <p>The path where to save the figure.</p> required <code>fmt</code> <code>str</code> <p>The export format (e.g. <code>\"png\"</code>, <code>\"svg\"</code>).</p> required"},{"location":"refs/backends/#plotmux.backends.base.check_export_format","title":"plotmux.backends.base.check_export_format","text":"<pre><code>check_export_format(\n    fmt: str, supported: Collection[str], backend_name: str\n) -&gt; None\n</code></pre> <p>Validate that <code>fmt</code> is one of <code>supported</code>.</p> <p>Parameters:</p> Name Type Description Default <code>fmt</code> <code>str</code> <p>The requested export format (e.g. <code>\"png\"</code>).</p> required <code>supported</code> <code>Collection[str]</code> <p>The formats the backend supports.</p> required <code>backend_name</code> <code>str</code> <p>The backend name, used only in the error message (e.g. <code>\"matplotlib\"</code>, <code>\"xy\"</code>).</p> required <p>Raises:</p> Type Description <code>UnsupportedFormatError</code> <p>if <code>fmt</code> is not in <code>supported</code>. Also a <code>ValueError</code>, so existing <code>except ValueError</code> code keeps working unchanged.</p>"},{"location":"refs/backends/#plotmux.backends.base.make_renderer","title":"plotmux.backends.base.make_renderer","text":"<pre><code>make_renderer(\n    chart_render: Callable[..., T],\n    style: Callable[[T, BaseSpec], T],\n) -&gt; Callable[..., T]\n</code></pre> <p>Build a <code>render(spec, **kwargs) -&gt; native</code> function that draws then styles in a single step.</p> <p>This suits backends with no separate figure/axes object to construct up front -- the chart-specific renderer already returns the finished native object, and styling (title, labels, scale) is applied to that same object afterwards. Both <code>altair</code> and <code>xy</code> follow this shape, so they share this helper instead of each defining their own near-identical wrapper.</p> <p>Backends that do need a construction step first (e.g. matplotlib building a <code>Figure</code>/<code>Axes</code> pair, or bokeh passing axis type at <code>figure()</code> construction time) define their own local <code>_make_renderer</code> instead, since that construction step is backend-specific and would otherwise leak into this shared helper.</p> <p>Parameters:</p> Name Type Description Default <code>chart_render</code> <code>Callable[..., T]</code> <p>The chart-specific <code>(spec, **kwargs) -&gt; native</code> renderer to wrap, e.g. <code>render_histogram</code>.</p> required <code>style</code> <code>Callable[[T, BaseSpec], T]</code> <p>The backend's <code>apply_common_style</code>-shaped function, called as <code>style(native, spec)</code> and returning the (possibly new, for immutable native objects) styled native object.</p> required <p>Returns:</p> Type Description <code>Callable[..., T]</code> <p>A <code>(spec, **kwargs) -&gt; native</code> renderer suitable for a backend's <code>_RENDERERS</code> dict.</p>"},{"location":"refs/backends/#plotmux.backends.base.resolve_renderer","title":"plotmux.backends.base.resolve_renderer","text":"<pre><code>resolve_renderer(\n    registry: dict[type[BaseSpec], Callable[..., T]],\n    spec: BaseSpec,\n    backend_name: str,\n) -&gt; Callable[..., T]\n</code></pre> <p>Look up the renderer registered for <code>type(spec)</code>.</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>dict[type[BaseSpec], Callable[..., T]]</code> <p>A <code>{spec_type: renderer}</code> dict, as owned by a backend's <code>_RENDERERS</code> or a layer module's <code>_AX_RENDERERS</code>/<code>_MARK_RENDERERS</code>.</p> required <code>spec</code> <code>BaseSpec</code> <p>The spec whose type is looked up.</p> required <code>backend_name</code> <code>str</code> <p>The backend name, used only in the error message (e.g. <code>\"matplotlib\"</code>, <code>\"xy\"</code>).</p> required <p>Returns:</p> Type Description <code>Callable[..., T]</code> <p>The renderer registered for <code>type(spec)</code>.</p> <p>Raises:</p> Type Description <code>UnsupportedSpecError</code> <p>if no renderer is registered for <code>type(spec)</code>. Also a <code>NotImplementedError</code>, so existing <code>except NotImplementedError</code> code keeps working unchanged.</p>"},{"location":"refs/backends/#plotmux.backends.registry","title":"plotmux.backends.registry","text":"<p>Contain the registry of available rendering backends.</p>"},{"location":"refs/backends/#plotmux.backends.registry.get_backend","title":"plotmux.backends.registry.get_backend","text":"<pre><code>get_backend(name: str) -&gt; Backend\n</code></pre> <p>Get a registered backend by name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the backend to retrieve.</p> required <p>Returns:</p> Type Description <code>Backend</code> <p>The registered backend instance.</p> <p>Raises:</p> Type Description <code>BackendNotFoundError</code> <p>if no backend is registered under <code>name</code>. This typically means the backend's underlying plotting library is not installed. Also a <code>RuntimeError</code>, so existing <code>except RuntimeError</code> code keeps working unchanged.</p>"},{"location":"refs/backends/#plotmux.backends.registry.known_backend_names","title":"plotmux.backends.registry.known_backend_names","text":"<pre><code>known_backend_names() -&gt; frozenset[str]\n</code></pre> <p>Return every backend name that could resolve, without importing or registering anything.</p> <p>This is the union of the built-in backend names (<code>_BUILTIN_BACKEND_MODULES</code>), the names advertised under the <code>plotmux.backends</code> entry-point group (querying <code>importlib.metadata.entry_points</code> is itself cheap: it reads installed packages' metadata, it does not import them), and whatever is already registered in <code>_REGISTRY</code> (covers a backend registered programmatically via <code>register_backend</code> outside both of the above, e.g. in a test or a script). Used by <code>plotmux.config.set_backend</code>/<code>backend</code> to reject a typo'd or nonexistent name immediately, at zero import cost, rather than only at the next render call (see <code>plotmux.backends.get_backend</code>): a name in this set may still fail at render time if its underlying plotting library is not installed, since being known is not the same as being registered.</p> <p>Returns:</p> Type Description <code>frozenset[str]</code> <p>The set of currently-resolvable backend names.</p>"},{"location":"refs/backends/#plotmux.backends.registry.load_entry_point_backends","title":"plotmux.backends.registry.load_entry_point_backends","text":"<pre><code>load_entry_point_backends() -&gt; None\n</code></pre> <p>Import every backend advertised via the <code>plotmux.backends</code> entry-point group.</p> <p>plotmux's own <code>matplotlib</code> and <code>xy</code> backends are wired in directly (see <code>plotmux.__init__</code>); this function is the plug-in mechanism for third-party backends, so a separate package can add a new backend without editing plotmux's source. To do so, a package declares an entry point of the form::</p> <pre><code>[project.entry-points.\"plotmux.backends\"]\nmy_backend = \"my_package.plotmux_backend\"\n</code></pre> <p>pointing at a module that calls <code>register_backend(...)</code> at import time (the same pattern used by <code>plotmux.backends.matplotlib</code>/<code>plotmux.backends.xy</code>). Calling this function imports every such module, which registers itself as a side effect.</p> <p>A module that fails to import because its own underlying plotting library is not installed (<code>ImportError</code>) is silently skipped, mirroring how the built-in backends guard their own registration behind an <code>is_..._available()</code> check. Any other exception raised while loading a plugin (a bug in the plugin itself, e.g. a broken <code>register_backend(...)</code> call) is caught and turned into a warning instead of propagating: a broken third-party plugin must never be able to crash <code>import plotmux</code> for every user, only fail to register itself.</p>"},{"location":"refs/backends/#plotmux.backends.registry.register_backend","title":"plotmux.backends.registry.register_backend","text":"<pre><code>register_backend(backend: Backend) -&gt; None\n</code></pre> <p>Register a backend instance under its <code>name</code>.</p> <p>Registering a backend under a name that is already registered replaces the previous registration.</p> <p>Parameters:</p> Name Type Description Default <code>backend</code> <code>Backend</code> <p>The backend instance to register.</p> required Example <pre><code>&gt;&gt;&gt; from plotmux.backends.registry import register_backend, get_backend\n&gt;&gt;&gt; class MyBackend:\n...     name = \"my_backend\"\n...\n&gt;&gt;&gt; register_backend(MyBackend())\n&gt;&gt;&gt; get_backend(\"my_backend\")  # doctest: +ELLIPSIS\n&lt;....MyBackend object at 0x...&gt;\n</code></pre>"},{"location":"refs/backends/#plotmux.backends.matplotlib.backend","title":"plotmux.backends.matplotlib.backend","text":"<p>Contain the matplotlib <code>Backend</code> implementation.</p> <p>This module is only imported when matplotlib is installed (see <code>plotmux.backends.matplotlib.__init__</code>), so it can import matplotlib unconditionally.</p>"},{"location":"refs/backends/#plotmux.backends.matplotlib.backend.MatplotlibBackend","title":"plotmux.backends.matplotlib.backend.MatplotlibBackend","text":"<p>               Bases: <code>Backend</code></p> <p>Implement the matplotlib rendering backend.</p> <p>One renderer function is registered per supported spec type in <code>_RENDERERS</code>. Adding a new chart type to this backend means adding one entry here; it never grows an if/elif chain.</p>"},{"location":"refs/backends/#plotmux.backends.matplotlib.backend.MatplotlibBackend.save","title":"plotmux.backends.matplotlib.backend.MatplotlibBackend.save","text":"<pre><code>save(native: Figure, path: Path, fmt: str) -&gt; None\n</code></pre> <p>Export a matplotlib <code>Figure</code> to a file.</p> <p>Parameters:</p> Name Type Description Default <code>native</code> <code>Figure</code> <p>The matplotlib <code>Figure</code> to export.</p> required <code>path</code> <code>Path</code> <p>The path where to save the figure.</p> required <code>fmt</code> <code>str</code> <p>The export format (e.g. <code>\"png\"</code>, <code>\"svg\"</code>).</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>fmt</code> is not a supported export format.</p>"},{"location":"refs/backends/#plotmux.backends.xy.backend","title":"plotmux.backends.xy.backend","text":"<p>Contain the xy <code>Backend</code> implementation.</p> <p>This module is only imported when xy is installed (see <code>plotmux.backends.xy.__init__</code>), so it can import xy unconditionally.</p>"},{"location":"refs/backends/#plotmux.backends.xy.backend.XyBackend","title":"plotmux.backends.xy.backend.XyBackend","text":"<p>               Bases: <code>Backend</code></p> <p>Implement the xy rendering backend.</p> <p>One renderer function is registered per supported spec type in <code>_RENDERERS</code>. Adding a new chart type to this backend means adding one entry here; it never grows an if/elif chain.</p>"},{"location":"refs/backends/#plotmux.backends.xy.backend.XyBackend.save","title":"plotmux.backends.xy.backend.XyBackend.save","text":"<pre><code>save(native: Chart | XyGrid, path: Path, fmt: str) -&gt; None\n</code></pre> <p>Export an xy <code>Chart</code> or <code>XyGrid</code> to a file.</p> <p>Parameters:</p> Name Type Description Default <code>native</code> <code>Chart | XyGrid</code> <p>The xy <code>Chart</code> (any spec type but <code>GridSpec</code>) or <code>XyGrid</code> (a rendered <code>GridSpec</code>, see <code>plotmux.backends.xy.grid</code>) to export.</p> required <code>path</code> <code>Path</code> <p>The path where to save the figure.</p> required <code>fmt</code> <code>str</code> <p>The export format (e.g. <code>\"png\"</code>, <code>\"svg\"</code>, <code>\"html\"</code>).</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>fmt</code> is not a supported export format, or <code>native</code> is an <code>XyGrid</code> and <code>fmt</code> is not <code>\"html\"</code> (see <code>XyGrid</code>'s docstring for why grid export is HTML-only).</p>"},{"location":"refs/backends/#plotmux.backends.bokeh.backend","title":"plotmux.backends.bokeh.backend","text":"<p>Contain the bokeh <code>Backend</code> implementation.</p> <p>This module is only imported when bokeh is installed (see <code>plotmux.backends.bokeh.__init__</code>), so it can import bokeh unconditionally.</p>"},{"location":"refs/backends/#plotmux.backends.bokeh.backend.BokehBackend","title":"plotmux.backends.bokeh.backend.BokehBackend","text":"<p>               Bases: <code>Backend</code></p> <p>Implement the bokeh rendering backend.</p> <p>One renderer function is registered per supported spec type in <code>_RENDERERS</code>. Adding a new chart type to this backend means adding one entry here; it never grows an if/elif chain.</p>"},{"location":"refs/backends/#plotmux.backends.bokeh.backend.BokehBackend.save","title":"plotmux.backends.bokeh.backend.BokehBackend.save","text":"<pre><code>save(\n    native: figure | LayoutDOM, path: Path, fmt: str\n) -&gt; None\n</code></pre> <p>Export a bokeh <code>figure</code> to a file.</p> <p>Parameters:</p> Name Type Description Default <code>native</code> <code>figure | LayoutDOM</code> <p>The bokeh <code>figure</code> to export.</p> required <code>path</code> <code>Path</code> <p>The path where to save the figure.</p> required <code>fmt</code> <code>str</code> <p>The export format. Only <code>\"html\"</code> is supported (see <code>supported_formats</code>).</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>fmt</code> is not a supported export format.</p>"},{"location":"refs/backends/#plotmux.backends.altair.backend","title":"plotmux.backends.altair.backend","text":"<p>Contain the altair <code>Backend</code> implementation.</p> <p>This module is only imported when altair is installed (see <code>plotmux.backends.altair.__init__</code>), so it can import altair unconditionally.</p>"},{"location":"refs/backends/#plotmux.backends.altair.backend.AltairBackend","title":"plotmux.backends.altair.backend.AltairBackend","text":"<p>               Bases: <code>Backend</code></p> <p>Implement the altair rendering backend.</p> <p>One renderer function is registered per supported spec type in <code>_RENDERERS</code>. Adding a new chart type to this backend means adding one entry here; it never grows an if/elif chain.</p>"},{"location":"refs/backends/#plotmux.backends.altair.backend.AltairBackend.save","title":"plotmux.backends.altair.backend.AltairBackend.save","text":"<pre><code>save(native: ChartType, path: Path, fmt: str) -&gt; None\n</code></pre> <p>Export an altair <code>Chart</code> to a file.</p> <p>Parameters:</p> Name Type Description Default <code>native</code> <code>ChartType</code> <p>The altair <code>Chart</code> to export.</p> required <code>path</code> <code>Path</code> <p>The path where to save the figure.</p> required <code>fmt</code> <code>str</code> <p>The export format. Only <code>\"html\"</code>/<code>\"json\"</code> are supported (see <code>supported_formats</code>).</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>fmt</code> is not a supported export format.</p>"},{"location":"refs/backends/#plotmux.backends.plotly.backend","title":"plotmux.backends.plotly.backend","text":"<p>Contain the plotly <code>Backend</code> implementation.</p> <p>This module is only imported when plotly is installed (see <code>plotmux.backends.plotly.__init__</code>), so it can import plotly unconditionally.</p>"},{"location":"refs/backends/#plotmux.backends.plotly.backend.PlotlyBackend","title":"plotmux.backends.plotly.backend.PlotlyBackend","text":"<p>               Bases: <code>Backend</code></p> <p>Implement the plotly rendering backend.</p> <p>One renderer function is registered per supported spec type in <code>_RENDERERS</code>. Adding a new chart type to this backend means adding one entry here; it never grows an if/elif chain.</p> <p><code>SlopeSpec</code> is deliberately absent: unlike matplotlib/bokeh, plotly has no native \"line by slope, independent of data range\" primitive, so (same as altair/xy) it is only supported as a <code>LayerSpec</code> child -- see <code>plotmux.backends.plotly.slope</code> and DESIGN.md, section 8.1.</p>"},{"location":"refs/backends/#plotmux.backends.plotly.backend.PlotlyBackend.save","title":"plotmux.backends.plotly.backend.PlotlyBackend.save","text":"<pre><code>save(native: Figure, path: Path, fmt: str) -&gt; None\n</code></pre> <p>Export a plotly <code>Figure</code> to a file.</p> <p>Parameters:</p> Name Type Description Default <code>native</code> <code>Figure</code> <p>The plotly <code>Figure</code> to export.</p> required <code>path</code> <code>Path</code> <p>The path where to save the figure.</p> required <code>fmt</code> <code>str</code> <p>The export format. <code>\"html\"</code> or <code>\"json\"</code> (see <code>supported_formats</code>).</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>fmt</code> is not a supported export format.</p>"},{"location":"refs/colors/","title":"Colors","text":""},{"location":"refs/colors/#plotmux.colors.parser","title":"plotmux.colors.parser","text":"<p>Contain a backend-agnostic color parser.</p> <p><code>parse_color</code> normalizes the color formats users already know (hex strings, CSS/matplotlib named colors, RGB(A) float tuples) into one canonical representation, an RGBA tuple of floats in <code>[0, 1]</code>. Each backend then converts that canonical tuple to whatever its native call expects (see <code>plotmux.backends.matplotlib.style</code> and <code>plotmux.backends.xy.style</code>).</p>"},{"location":"refs/colors/#plotmux.colors.parser.parse_color","title":"plotmux.colors.parser.parse_color","text":"<pre><code>parse_color(\n    color: (\n        str\n        | tuple[float, float, float]\n        | tuple[float, float, float, float]\n    ),\n) -&gt; tuple[float, float, float, float]\n</code></pre> <p>Parse a color into a canonical RGBA tuple of floats in <code>[0, 1]</code>.</p> <p>Parameters:</p> Name Type Description Default <code>color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float]</code> <p>The color to parse. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>, <code>\"crimson\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>.</p> required <p>Returns:</p> Type Description <code>tuple[float, float, float, float]</code> <p>The color as an <code>(r, g, b, a)</code> tuple of floats in <code>[0, 1]</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>color</code> is not a valid color.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.colors import parse_color\n&gt;&gt;&gt; parse_color(\"#ff0000\")\n(1.0, 0.0, 0.0, 1.0)\n&gt;&gt;&gt; parse_color((0.5, 0.5, 0.5))\n(0.5, 0.5, 0.5, 1.0)\n</code></pre>"},{"location":"refs/colors/#plotmux.colors.palette","title":"plotmux.colors.palette","text":"<p>Contain predefined colors and a default categorical palette.</p> <p>Every value here is already a <code>parse_color</code>-normalized RGBA tuple of floats in <code>[0, 1]</code>, so callers and backends never need to re-parse them (same \"canonical input, one parser, reused everywhere\" pattern as <code>parse_color</code> itself \u2014 see <code>plotmux.colors.parser</code>).</p>"},{"location":"refs/config/","title":"Config","text":""},{"location":"refs/config/#plotmux.config","title":"plotmux.config","text":"<p>Contain the default-backend configuration.</p>"},{"location":"refs/config/#plotmux.config.backend","title":"plotmux.config.backend","text":"<pre><code>backend(name: str) -&gt; Iterator[None]\n</code></pre> <p>Temporarily override the default backend.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the backend to use within the <code>with</code> block.</p> required <p>Yields:</p> Type Description <code>None</code> <p>Nothing. Restores the previous default backend on exit.</p> Example <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; with plotmux.backend(\"matplotlib\"):\n...     fig = plotmux.hist([1, 2, 3])\n...\n</code></pre>"},{"location":"refs/config/#plotmux.config.get_default_backend","title":"plotmux.config.get_default_backend","text":"<pre><code>get_default_backend() -&gt; str\n</code></pre> <p>Get the name of the current default backend.</p> <p>Returns:</p> Type Description <code>str</code> <p>The name of the current default backend.</p>"},{"location":"refs/config/#plotmux.config.set_backend","title":"plotmux.config.set_backend","text":"<pre><code>set_backend(name: str) -&gt; None\n</code></pre> <p>Set the default backend for the current thread/task.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the backend to use by default, e.g. <code>\"matplotlib\"</code>.</p> required <p>Raises:</p> Type Description <code>BackendNotFoundError</code> <p>if <code>name</code> is not a known backend name, i.e. it is neither a built-in backend, nor advertised by an installed third-party plugin via the <code>plotmux.backends</code> entry-point group, nor already registered. This check costs no import: it only compares <code>name</code> against the known set (see <code>plotmux.backends.registry.known_backend_names</code>), so a typo'd name is caught here, immediately, instead of only surfacing on the next <code>plotmux.hist(...)</code>/etc. call. A name that passes this check can still fail later, at render time, if its underlying plotting library turns out not to be installed: being known is not the same as being registered.</p> Example <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; plotmux.set_backend(\"matplotlib\")\n</code></pre>"},{"location":"refs/exceptions/","title":"Exceptions","text":""},{"location":"refs/exceptions/#plotmux.exceptions","title":"plotmux.exceptions","text":"<p>Contain plotmux's exception hierarchy.</p> <p>Every exception plotmux itself raises is a <code>PlotmuxError</code>, in addition to whichever standard-library exception type the raise site already documented (e.g. <code>ValueError</code>, <code>RuntimeError</code>). Each concrete exception below multiply-inherits from both: <code>except ValueError</code> (or <code>RuntimeError</code>/<code>NotImplementedError</code>) at an existing call site keeps working exactly as before, while new code can catch anything plotmux-specific in one place with <code>except PlotmuxError</code>, without having to know or enumerate which builtin type backs each individual error.</p> <p>Only the exception type changes here, never the raised message or the condition that triggers it -- this is purely an additive refinement of what was already being raised.</p>"},{"location":"refs/exceptions/#plotmux.exceptions.BackendNotFoundError","title":"plotmux.exceptions.BackendNotFoundError","text":"<p>               Bases: <code>PlotmuxError</code>, <code>RuntimeError</code></p> <p>Raised when a backend name has no registered <code>Backend</code>.</p> <p>Typically means the backend's underlying plotting library is not installed. See <code>plotmux.backends.registry.get_backend</code>.</p>"},{"location":"refs/exceptions/#plotmux.exceptions.ExportError","title":"plotmux.exceptions.ExportError","text":"<p>               Bases: <code>PlotmuxError</code>, <code>ValueError</code></p> <p>Raised when a figure cannot be exported, other than an unsupported format.</p> <p>See <code>plotmux.export.save</code>.</p>"},{"location":"refs/exceptions/#plotmux.exceptions.InvalidColorError","title":"plotmux.exceptions.InvalidColorError","text":"<p>               Bases: <code>PlotmuxError</code>, <code>ValueError</code></p> <p>Raised when a color value cannot be parsed.</p> <p>See <code>plotmux.colors.parse_color</code>.</p>"},{"location":"refs/exceptions/#plotmux.exceptions.InvalidSpecError","title":"plotmux.exceptions.InvalidSpecError","text":"<p>               Bases: <code>PlotmuxError</code>, <code>ValueError</code></p> <p>Raised when a spec's field values fail validation.</p> <p>See each spec's <code>__post_init__</code> (e.g. <code>plotmux.specs.HistogramSpec</code>, <code>plotmux.specs.LineSpec</code>).</p>"},{"location":"refs/exceptions/#plotmux.exceptions.PlotmuxError","title":"plotmux.exceptions.PlotmuxError","text":"<p>               Bases: <code>Exception</code></p> <p>Base class for every exception raised by plotmux.</p> <p>Catch this to handle any plotmux-specific failure without having to know which of the more specific subclasses (or which builtin type each one also derives from) applies.</p>"},{"location":"refs/exceptions/#plotmux.exceptions.UnsupportedFormatError","title":"plotmux.exceptions.UnsupportedFormatError","text":"<p>               Bases: <code>PlotmuxError</code>, <code>ValueError</code></p> <p>Raised when a backend does not support a requested export format.</p> <p>See <code>plotmux.backends.base.check_export_format</code>.</p>"},{"location":"refs/exceptions/#plotmux.exceptions.UnsupportedSpecError","title":"plotmux.exceptions.UnsupportedSpecError","text":"<p>               Bases: <code>PlotmuxError</code>, <code>NotImplementedError</code></p> <p>Raised when a backend has no renderer registered for a spec type.</p> <p>See <code>plotmux.backends.base.resolve_renderer</code>.</p>"},{"location":"refs/export/","title":"Export","text":""},{"location":"refs/export/#plotmux.export","title":"plotmux.export","text":"<p>Contain figure export utilities.</p>"},{"location":"refs/export/#plotmux.export.save","title":"plotmux.export.save","text":"<pre><code>save(figure: Figure, path: str | Path) -&gt; None\n</code></pre> <p>Save a figure to a file.</p> <p>The export format is inferred from the file suffix (e.g. <code>.png</code> -&gt; <code>\"png\"</code>, <code>.svg</code> -&gt; <code>\"svg\"</code>).</p> <p>The parent directory of <code>path</code> is created if it does not already exist.</p> <p>Parameters:</p> Name Type Description Default <code>figure</code> <code>Figure</code> <p>The figure to save.</p> required <code>path</code> <code>str | Path</code> <p>The path where to save the figure.</p> required <p>Raises:</p> Type Description <code>ExportError</code> <p>if <code>path</code> has no suffix, so the export format cannot be inferred. Also a <code>ValueError</code>, so existing <code>except ValueError</code> code keeps working unchanged.</p> <code>UnsupportedFormatError</code> <p>if the backend does not support the inferred export format. See <code>plotmux.backends.base.check_export_format</code>.</p>"},{"location":"refs/figure/","title":"Figure","text":""},{"location":"refs/figure/#plotmux.figure","title":"plotmux.figure","text":"<p>Contain the <code>Figure</code> wrapper returned by the public API.</p>"},{"location":"refs/figure/#plotmux.figure.Figure","title":"plotmux.figure.Figure  <code>dataclass</code>","text":"<p>Wrap the result of rendering a spec through a backend.</p> <p><code>Figure</code> is the object returned to the user by the public API (<code>plotmux.hist</code>, <code>plotmux.line</code>, <code>plotmux.scatter</code>, ...). It keeps the spec that produced it, the name of the backend that rendered it, and the backend's native figure object.</p> <p>Parameters:</p> Name Type Description Default <code>spec</code> <code>BaseSpec</code> <p>The spec that was rendered.</p> required <code>backend_name</code> <code>str</code> <p>The name of the backend that rendered <code>native</code>.</p> required <code>native</code> <code>Any</code> <p>The backend's native figure object (e.g. a matplotlib <code>Figure</code>).</p> required"},{"location":"refs/figure/#plotmux.figure.Figure.supported_formats","title":"plotmux.figure.Figure.supported_formats  <code>property</code>","text":"<pre><code>supported_formats: frozenset[str]\n</code></pre> <p>The export formats accepted by <code>save</code> for this figure's backend.</p> <p>Different backends support different export formats (e.g. the <code>bokeh</code> backend only supports <code>\"html\"</code>, since static image export requires an extra, environment-specific dependency). This lets callers check what is supported ahead of time instead of discovering it via a <code>ValueError</code> from <code>save</code>.</p> <p>Returns:</p> Type Description <code>frozenset[str]</code> <p>The set of export formats accepted by <code>save</code>.</p>"},{"location":"refs/figure/#plotmux.figure.Figure.__getattr__","title":"plotmux.figure.Figure.__getattr__","text":"<pre><code>__getattr__(name: str) -&gt; Any\n</code></pre> <p>Forward IPython/Jupyter rich-display lookups to <code>native</code>.</p> <p>This is what makes a <code>Figure</code> display itself automatically as the last expression of a Jupyter cell, exactly as its <code>native</code> object would on its own -- without every backend having to reimplement its own <code>_repr_html_</code>/... wrapper, and without <code>Figure</code> hardcoding which of those methods a given backend's native object happens to define.</p> <p>Only called by Python when normal attribute lookup on <code>Figure</code> itself fails, so it never shadows a real <code>Figure</code> attribute or method. Restricted to the fixed <code>_REPR_METHODS</code> set (rather than forwarding any missing attribute) so a typo in user code (e.g. <code>fig.sav(...)</code>) still raises a clear <code>AttributeError</code> instead of silently forwarding to <code>native</code> and failing there with a confusing error.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The attribute name that was looked up.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The bound method of the same name on <code>native</code>.</p> <p>Raises:</p> Type Description <code>AttributeError</code> <p>if <code>name</code> is not a known rich-display method, or <code>native</code> does not define it.</p>"},{"location":"refs/figure/#plotmux.figure.Figure.save","title":"plotmux.figure.Figure.save","text":"<pre><code>save(path: str | Path) -&gt; None\n</code></pre> <p>Save the figure to a file.</p> <p>The export format is inferred from the file suffix (e.g. <code>.png</code>, <code>.svg</code>).</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>The path where to save the figure.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>path</code> has no suffix, so the export format cannot be inferred.</p>"},{"location":"refs/figure/#plotmux.figure.Figure.show","title":"plotmux.figure.Figure.show","text":"<pre><code>show() -&gt; None\n</code></pre> <p>Display the figure using the backend's default viewer.</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>if the backend's native figure object does not expose a <code>show</code> method.</p>"},{"location":"refs/figure/#plotmux.figure.Figure.to_native","title":"plotmux.figure.Figure.to_native","text":"<pre><code>to_native() -&gt; Any\n</code></pre> <p>Return the backend's native figure object.</p> <p>This is the escape hatch to reach backend-specific functionality that is not exposed by the unified API.</p> <p>Returns:</p> Type Description <code>Any</code> <p>The backend's native figure object.</p>"},{"location":"refs/specs/","title":"Specs","text":""},{"location":"refs/specs/#plotmux.specs.base","title":"plotmux.specs.base","text":"<p>Contain the base class for backend-agnostic chart specifications.</p>"},{"location":"refs/specs/#plotmux.specs.base.BaseSpec","title":"plotmux.specs.base.BaseSpec  <code>dataclass</code>","text":"<p>Define the base class for chart specifications.</p> <p>A spec is a plain, backend-agnostic description of what to plot (data + encoding + style). It must never import or hold a reference to a plotting library object. Turning a spec into a native figure is the responsibility of a backend (see <code>plotmux.backends</code>).</p> <p>Subclasses are expected to be frozen dataclasses and may use <code>__post_init__</code> to validate their fields.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str | None</code> <p>An optional figure title.</p> <code>None</code> <code>xlabel</code> <code>str | None</code> <p>An optional x-axis label.</p> <code>None</code> <code>ylabel</code> <code>str | None</code> <p>An optional y-axis label.</p> <code>None</code> <code>xscale</code> <code>Literal['linear', 'log']</code> <p>The x-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>yscale</code> <code>Literal['linear', 'log']</code> <p>The y-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>.</p> <code>'linear'</code> <code>background_color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional figure background color. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default (usually white/transparent). See <code>plotmux.colors.parse_color</code> for the exact semantics.</p> <code>None</code> <code>ymin</code> <code>float | None</code> <p>An optional lower bound for the y-axis. Unlike <code>HistogramSpec.xmin</code>/<code>CdfSpec.xmin</code> (see <code>plotmux.utils.range.find_range</code>), this is an explicit value only, not a quantile string: it is figure-level (every chart type has a y-axis; not every chart type has a single data array to resolve a quantile against), so it is applied as a plain axis bound after the mark is drawn, rather than folded into any one renderer's own data processing. <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>ymax</code> <code>float | None</code> <p>An optional upper bound for the y-axis. Same semantics as <code>ymin</code> but for the upper bound.</p> <code>None</code> <code>xmin</code> <code>float | None</code> <p>An optional lower bound for the x-axis. Like <code>ymin</code>/<code>ymax</code>, this is a plain, explicit-value-only figure-level override (not the quantile-string form <code>HistogramSpec.xmin</code>/<code>CdfSpec.xmin</code> accept -- see <code>plotmux.utils.range.find_range</code> -- those two remain data-scoped and quantile-capable; this field is open to every chart type). <code>None</code> leaves the axis autoscaled.</p> <code>None</code> <code>xmax</code> <code>float | None</code> <p>An optional upper bound for the x-axis. Same semantics as <code>xmin</code> but for the upper bound.</p> <code>None</code> <code>legend_title</code> <code>str | None</code> <p>An optional heading for the legend box itself, independent of <code>title</code> (the figure/axes title). <code>None</code> leaves the legend untitled (every backend's own default). Only meaningful when at least one mark carries a <code>label</code> -- a spec with <code>legend_title</code> set but no labeled mark simply draws no legend at all, same as today.</p> <code>None</code> <code>legend_location</code> <code>Literal['best', 'top_left', 'top_right', 'bottom_left', 'bottom_right', 'top', 'bottom', 'left', 'right'] | None</code> <p>An optional legend position, one of a small, backend-portable set of named corners/edges (mirroring how <code>ScatterSpec.marker</code> exposes a portable shape set rather than every backend's native vocabulary). <code>None</code> leaves the legend at the backend's own default position. Only meaningful when at least one mark carries a <code>label</code>, same caveat as <code>legend_title</code>.</p> <code>None</code> <p>These are figure-level concerns shared by every chart type, so they live here rather than being redeclared per chart type. Every backend applies them the same way, in one shared <code>apply_common_style</code> helper, right after drawing the chart-specific mark (see e.g. <code>plotmux.backends.matplotlib.style.apply_common_style</code>). Unlike <code>title</code>/<code>xlabel</code>/<code>ylabel</code>, <code>xscale</code>/<code>yscale</code> default to <code>\"linear\"</code> rather than <code>None</code>: an axis always has some scale, so there is no meaningful \"unset\" state to skip.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>background_color</code> is not a valid color, or <code>ymin</code>/<code>ymax</code> are both set with <code>ymin &gt; ymax</code>, or <code>xmin</code>/<code>xmax</code> are both set with <code>xmin &gt; xmax</code>.</p>"},{"location":"refs/specs/#plotmux.specs.histogram","title":"plotmux.specs.histogram","text":"<p>Contain the backend-agnostic histogram specification.</p>"},{"location":"refs/specs/#plotmux.specs.histogram.HistogramSpec","title":"plotmux.specs.histogram.HistogramSpec  <code>dataclass</code>","text":"<p>               Bases: <code>BaseSpec</code></p> <p>Define a backend-agnostic histogram specification.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>ndarray</code> <p>The array of values to plot.</p> required <code>bins</code> <code>int</code> <p>The number of histogram bins. Must be a positive integer.</p> <code>30</code> <code>xmin</code> <code>float | str | None</code> <p>Specifies the lower bound of the x-axis range. It can be an explicit value, a quantile string such as <code>\"q0.1\"</code>, or <code>None</code> to use the minimum of <code>values</code>. See <code>plotmux.utils.range.find_range</code> for the exact semantics.</p> <code>None</code> <code>xmax</code> <code>float | str | None</code> <p>Specifies the upper bound of the x-axis range. Same semantics as <code>xmin</code> but for the upper bound.</p> <code>None</code> <code>label</code> <code>str | None</code> <p>An optional label used e.g. in the legend.</p> <code>None</code> <code>density</code> <code>bool</code> <p>If <code>True</code>, draw and return a probability density: each bin will display the bin's raw count divided by the total number of counts and the bin width, so that the area under the histogram integrates to 1. Defaults to <code>False</code>.</p> <code>False</code> <code>color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional color for the bars. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default color. See <code>plotmux.colors.parse_color</code> for the exact semantics.</p> <code>None</code> <code>alpha</code> <code>float | None</code> <p>An optional bar opacity, in <code>[0, 1]</code>. <code>None</code> uses the backend's default (usually fully opaque).</p> <code>None</code> <code>title</code> <code>str | None</code> <p>An optional figure title. Inherited from <code>BaseSpec</code>.</p> <code>None</code> <code>xlabel</code> <code>str | None</code> <p>An optional x-axis label. Inherited from <code>BaseSpec</code>.</p> <code>None</code> <code>ylabel</code> <code>str | None</code> <p>An optional y-axis label. Inherited from <code>BaseSpec</code>.</p> <code>None</code> <code>xscale</code> <code>Literal['linear', 'log']</code> <p>The x-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>. Inherited from <code>BaseSpec</code>.</p> <code>'linear'</code> <code>yscale</code> <code>Literal['linear', 'log']</code> <p>The y-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>. Inherited from <code>BaseSpec</code>.</p> <code>'linear'</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>bins</code> is not a positive integer, <code>values</code> is not 1-dimensional or is empty, <code>color</code> is not a valid color, or <code>xmin</code> and <code>xmax</code> are both explicit numeric values with <code>xmin &gt;= xmax</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from plotmux.specs import HistogramSpec\n&gt;&gt;&gt; spec = HistogramSpec(values=np.arange(101), bins=10)\n&gt;&gt;&gt; spec.bins\n10\n</code></pre>"},{"location":"refs/specs/#plotmux.specs.cdf","title":"plotmux.specs.cdf","text":"<p>Contain the backend-agnostic empirical CDF specification.</p>"},{"location":"refs/specs/#plotmux.specs.cdf.CdfSpec","title":"plotmux.specs.cdf.CdfSpec  <code>dataclass</code>","text":"<p>               Bases: <code>BaseSpec</code></p> <p>Define a backend-agnostic empirical cumulative distribution function (CDF) specification.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>ndarray</code> <p>The array of values to plot.</p> required <code>nbins</code> <code>int | None</code> <p>The number of bins to use to approximate the cumulative distribution. Must be a positive integer, or <code>None</code> to use the backend's default binning.</p> <code>None</code> <code>xmin</code> <code>float | str | None</code> <p>Specifies the lower bound of the x-axis range. It can be an explicit value, a quantile string such as <code>\"q0.1\"</code>, or <code>None</code> to use the minimum of <code>values</code>. See <code>plotmux.utils.range.find_range</code> for the exact semantics.</p> <code>None</code> <code>xmax</code> <code>float | str | None</code> <p>Specifies the upper bound of the x-axis range. Same semantics as <code>xmin</code> but for the upper bound.</p> <code>None</code> <code>label</code> <code>str | None</code> <p>An optional label used e.g. in the legend.</p> <code>None</code> <code>color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional color for the curve. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default color. See <code>plotmux.colors.parse_color</code> for the exact semantics.</p> <code>None</code> <code>alpha</code> <code>float | None</code> <p>An optional curve opacity, in <code>[0, 1]</code>. <code>None</code> uses the backend's default (usually fully opaque).</p> <code>None</code> <code>title</code> <code>str | None</code> <p>An optional figure title. Inherited from <code>BaseSpec</code>.</p> <code>None</code> <code>xlabel</code> <code>str | None</code> <p>An optional x-axis label. Inherited from <code>BaseSpec</code>.</p> <code>None</code> <code>ylabel</code> <code>str | None</code> <p>An optional y-axis label. Defaults to <code>\"cumulative probability\"</code> rather than <code>BaseSpec</code>'s usual <code>None</code>, since a CDF's y-axis always represents that same quantity unless the caller overrides it.</p> <code>'cumulative probability'</code> <code>xscale</code> <code>Literal['linear', 'log']</code> <p>The x-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>. Inherited from <code>BaseSpec</code>.</p> <code>'linear'</code> <code>yscale</code> <code>Literal['linear', 'log']</code> <p>The y-axis scale, <code>\"linear\"</code> or <code>\"log\"</code>. Inherited from <code>BaseSpec</code>.</p> <code>'linear'</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>nbins</code> is set and is not a positive integer, <code>values</code> is not 1-dimensional or is empty, <code>color</code> is not a valid color, or <code>xmin</code> and <code>xmax</code> are both explicit numeric values with <code>xmin &gt;= xmax</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from plotmux.specs import CdfSpec\n&gt;&gt;&gt; spec = CdfSpec(values=np.arange(101), nbins=10)\n&gt;&gt;&gt; spec.nbins\n10\n</code></pre>"},{"location":"refs/specs/#plotmux.specs.line","title":"plotmux.specs.line","text":"<p>Contain the backend-agnostic line-chart specification.</p>"},{"location":"refs/specs/#plotmux.specs.line.LineSpec","title":"plotmux.specs.line.LineSpec  <code>dataclass</code>","text":"<p>               Bases: <code>BaseSpec</code></p> <p>Define a backend-agnostic line-chart specification.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ndarray</code> <p>The array of x values.</p> required <code>y</code> <code>ndarray</code> <p>The array of y values. Must have the same length as <code>x</code>.</p> required <code>label</code> <code>str | None</code> <p>An optional label used e.g. in the legend.</p> <code>None</code> <code>color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional color for the line. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default color. See <code>plotmux.colors.parse_color</code> for the exact semantics.</p> <code>None</code> <code>alpha</code> <code>float | None</code> <p>An optional line opacity, in <code>[0, 1]</code>. <code>None</code> uses the backend's default (usually fully opaque).</p> <code>None</code> <code>linewidth</code> <code>float | None</code> <p>An optional line width. <code>None</code> uses the backend's default width. Same field name/semantics as <code>SlopeSpec.linewidth</code>.</p> <code>None</code> <code>linestyle</code> <code>Literal['solid', 'dashed', 'dotted', 'dashdot']</code> <p>The line's dash style. Same field name/semantics as <code>SlopeSpec.linestyle</code>.</p> <code>'solid'</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>x</code> and <code>y</code> do not have the same length, <code>alpha</code> is not in <code>[0, 1]</code>, or <code>color</code> is not a valid color.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from plotmux.specs import LineSpec\n&gt;&gt;&gt; spec = LineSpec(x=np.arange(10), y=np.arange(10) ** 2)\n&gt;&gt;&gt; spec.x.shape\n(10,)\n</code></pre>"},{"location":"refs/specs/#plotmux.specs.scatter","title":"plotmux.specs.scatter","text":"<p>Contain the backend-agnostic scatter-chart specification.</p>"},{"location":"refs/specs/#plotmux.specs.scatter.ScatterSpec","title":"plotmux.specs.scatter.ScatterSpec  <code>dataclass</code>","text":"<p>               Bases: <code>BaseSpec</code></p> <p>Define a backend-agnostic scatter-chart specification.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ndarray</code> <p>The array of x values.</p> required <code>y</code> <code>ndarray</code> <p>The array of y values. Must have the same length as <code>x</code>.</p> required <code>label</code> <code>str | None</code> <p>An optional label used e.g. in the legend.</p> <code>None</code> <code>color</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional color for the markers. It can be a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/matplotlib named color (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. <code>None</code> uses the backend's default color. See <code>plotmux.colors.parse_color</code> for the exact semantics.</p> <code>None</code> <code>size</code> <code>float | None</code> <p>An optional marker size. <code>None</code> uses the backend's default size.</p> <code>None</code> <code>edgecolor</code> <code>str | tuple[float, float, float] | tuple[float, float, float, float] | None</code> <p>An optional, separate color for the marker edge (as opposed to <code>color</code>, which fills the marker). Same format as <code>color</code>. <code>None</code> uses <code>color</code> for the edge too (every backend's renderer already does this when <code>edgecolor</code> is unset -- see e.g. <code>plotmux.backends.bokeh.scatter.render_scatter</code>), so a plain, single-color marker still needs only <code>color</code>.</p> <code>None</code> <code>alpha</code> <code>float | None</code> <p>An optional marker opacity, in <code>[0, 1]</code>. <code>None</code> uses the backend's default (usually fully opaque).</p> <code>None</code> <code>marker</code> <code>Literal['circle', 'square', 'triangle', 'diamond', 'cross', 'x'] | None</code> <p>An optional marker shape, one of a small, backend-portable set (mirroring how <code>LineSpec.linestyle</code> exposes four portable names rather than every backend's native dash vocabulary). <code>None</code> uses the backend's default shape (a circle on every current backend). altair has no native <code>\"x\"</code> shape (see <code>plotmux.backends.altair.style.MARKER_STYLE</code>), a small, permanent per-backend asymmetry, the same pattern as <code>BarSpec.width</code>'s altair gap.</p> <code>None</code> <code>fill</code> <code>bool | None</code> <p>An optional tri-state fill toggle. <code>None</code>/<code>True</code> draw a filled marker using <code>color</code>, today's behavior. <code>False</code> draws a hollow marker: only the outline ( <code>edgecolor</code> if set, else <code>color</code>) is drawn, with no fill -- distinct from leaving <code>color</code> unset, which asks for the backend's own default (opaque) fill color rather than an explicitly transparent one. Every current backend's own default fill is opaque, so <code>fill=False</code> is the only portable way to get a hollow marker.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>x</code> and <code>y</code> do not have the same length, <code>size</code> is not a positive number, <code>alpha</code> is not in <code>[0, 1]</code>, or <code>color</code>/<code>edgecolor</code> is not a valid color.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from plotmux.specs import ScatterSpec\n&gt;&gt;&gt; spec = ScatterSpec(x=np.arange(10), y=np.arange(10) ** 2)\n&gt;&gt;&gt; spec.x.shape\n(10,)\n</code></pre>"},{"location":"refs/specs/#plotmux.specs.layer","title":"plotmux.specs.layer","text":"<p>Contain the backend-agnostic layering specification.</p>"},{"location":"refs/specs/#plotmux.specs.layer.LayerSpec","title":"plotmux.specs.layer.LayerSpec  <code>dataclass</code>","text":"<p>               Bases: <code>BaseSpec</code></p> <p>Define a spec that draws multiple child specs on one shared axes.</p> <p>Parameters:</p> Name Type Description Default <code>layers</code> <code>tuple[BaseSpec, ...]</code> <p>The child specs to draw together, in draw order. Must be non-empty. A <code>LayerSpec</code> nested inside <code>layers</code> is rejected: layering is designed as one flat pass over <code>layers</code> (see <code>plotmux.backends.matplotlib.layer.render_layer</code> and <code>plotmux.backends.xy.layer.render_layer</code>), so callers must flatten nested layers themselves rather than relying on recursive dispatch.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>layers</code> is empty or contains a <code>LayerSpec</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from plotmux.specs import HistogramSpec, LayerSpec, LineSpec\n&gt;&gt;&gt; spec = LayerSpec(\n...     layers=(\n...         HistogramSpec(values=np.arange(101), bins=10),\n...         LineSpec(x=np.arange(10), y=np.arange(10)),\n...     )\n... )\n&gt;&gt;&gt; len(spec.layers)\n2\n</code></pre>"},{"location":"refs/specs/#plotmux.specs.grid","title":"plotmux.specs.grid","text":"<p>Contain the backend-agnostic grid-layout specification.</p>"},{"location":"refs/specs/#plotmux.specs.grid.GridSpec","title":"plotmux.specs.grid.GridSpec  <code>dataclass</code>","text":"<p>               Bases: <code>BaseSpec</code></p> <p>Define a spec that lays out multiple child specs as independent panels in a grid.</p> <p>Unlike <code>LayerSpec</code> (multiple specs drawn onto one shared axes, see <code>plotmux.specs.LayerSpec</code>), each child here gets its own, independent panel -- the backend-agnostic equivalent of matplotlib's <code>pyplot.subplots</code>. A child may itself be a <code>LayerSpec</code> (several series sharing one panel's axes); a nested <code>GridSpec</code> is rejected for the same reason <code>LayerSpec</code> rejects nesting: layout is one flat pass over <code>cells</code>, so callers must flatten nested grids themselves.</p> <p>Parameters:</p> Name Type Description Default <code>cells</code> <code>tuple[BaseSpec, ...]</code> <p>The child specs to draw, one independent panel per spec, in row-major order (left to right, top to bottom). Must be non-empty.</p> required <code>ncols</code> <code>int</code> <p>The number of columns in the grid. Rows are filled left to right; the last row is left short (its remaining panels left empty) when <code>len(cells)</code> is not a multiple of <code>ncols</code>. Must be a positive integer.</p> <code>1</code> <code>title</code> <code>str | None</code> <p>An optional figure-level title, shown once above the whole grid (not any individual cell). Inherited from <code>BaseSpec</code>.</p> <code>None</code> <p><code>xlabel</code>/<code>ylabel</code>/<code>xscale</code>/<code>yscale</code>, also inherited from <code>BaseSpec</code>, have no meaning at the grid level -- each cell keeps its own -- so every backend's grid renderer ignores them.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>cells</code> is empty, contains a <code>GridSpec</code>, or <code>ncols</code> is not a positive integer.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from plotmux.specs import GridSpec, HistogramSpec, LineSpec\n&gt;&gt;&gt; spec = GridSpec(\n...     cells=(\n...         HistogramSpec(values=np.arange(101), bins=10),\n...         LineSpec(x=np.arange(10), y=np.arange(10)),\n...     ),\n...     ncols=2,\n... )\n&gt;&gt;&gt; len(spec.cells)\n2\n</code></pre>"},{"location":"refs/testing/","title":"Testing","text":""},{"location":"refs/testing/#plotmux.testing.fixtures","title":"plotmux.testing.fixtures","text":"<p>Define <code>pytest</code> markers to skip tests based on the availability of <code>plotmux</code>'s optional backend dependencies (<code>altair</code>, <code>bokeh</code>, <code>matplotlib</code>, <code>plotly</code>, <code>xy</code>).</p> <p><code>pytest</code> is required to use these fixtures. Each <code>&lt;package&gt;_available</code> marker skips the test unless the package is installed, and each <code>&lt;package&gt;_not_available</code> marker skips the test if the package is installed, which is useful to test the \"missing dependency\" error paths (see <code>plotmux.utils.imports</code>).</p> Example <pre><code>&gt;&gt;&gt; from plotmux.testing.fixtures import matplotlib_available\n&gt;&gt;&gt; @matplotlib_available\n... def test_something() -&gt; None:\n...     pass\n...\n</code></pre>"},{"location":"refs/utils/","title":"Utils","text":""},{"location":"refs/utils/#plotmux.utils.range","title":"plotmux.utils.range","text":"<p>Contain utility functions to manage ranges of values.</p>"},{"location":"refs/utils/#plotmux.utils.range.find_range","title":"plotmux.utils.range.find_range","text":"<pre><code>find_range(\n    values: ndarray,\n    xmin: float | str | None = None,\n    xmax: float | str | None = None,\n) -&gt; tuple[float, float]\n</code></pre> <p>Find a valid range of values.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>ndarray</code> <p>The array of values used to compute the quantiles.</p> required <code>xmin</code> <code>float | str | None</code> <p>Specifies the lower bound of the range. It can be an explicit value, a quantile string such as <code>\"q0.1\"</code> (i.e. the 10% quantile, where <code>\"q0\"</code> is the minimum value and <code>\"q1\"</code> is the maximum value), or <code>None</code> to use the minimum of <code>values</code>.</p> <code>None</code> <code>xmax</code> <code>float | str | None</code> <p>Specifies the upper bound of the range. It can be an explicit value, a quantile string such as <code>\"q0.9\"</code> (i.e. the 90% quantile, where <code>\"q0\"</code> is the minimum value and <code>\"q1\"</code> is the maximum value), or <code>None</code> to use the maximum of <code>values</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple[float, float]</code> <p>The range of values as a <code>(min, max)</code> tuple. <code>(nan, nan)</code> is returned if <code>values</code> is empty.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>xmin</code> or <code>xmax</code> is a string that does not have the format <code>\"q&lt;quantile&gt;\"</code> where <code>&lt;quantile&gt;</code> is a float in the range <code>[0, 1]</code>, or if the resolved lower bound is strictly greater than the resolved upper bound (e.g. <code>xmin=\"q0.9\", xmax=\"q0.1\"</code>).</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from plotmux.utils import find_range\n&gt;&gt;&gt; data = np.arange(101)\n&gt;&gt;&gt; find_range(data)\n(0, 100)\n&gt;&gt;&gt; find_range(data, xmin=5, xmax=50)\n(5.0, 50.0)\n&gt;&gt;&gt; find_range(data, xmin=\"q0.1\", xmax=\"q0.9\")\n(10.0, 90.0)\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.cdf","title":"plotmux.utils.cdf","text":"<p>Contain utility functions to compute an empirical CDF's step coordinates.</p>"},{"location":"refs/utils/#plotmux.utils.cdf.compute_cdf_steps","title":"plotmux.utils.cdf.compute_cdf_steps","text":"<pre><code>compute_cdf_steps(\n    values: ndarray, bins: int, xmin: float, xmax: float\n) -&gt; tuple[ndarray, ndarray]\n</code></pre> <p>Compute the <code>(x, y)</code> vertices of a binned empirical CDF's step curve.</p> <p>Bin counts are computed with <code>numpy.histogram</code> over <code>(xmin, xmax)</code> and turned into a cumulative, density-normalized curve, the same approach matplotlib's own <code>Axes.hist(..., density=True, cumulative=True, histtype=\"step\")</code> takes internally (see <code>plotmux.backends.matplotlib.cdf.render_cdf</code>, which uses that call directly instead of this helper). bokeh/altair/xy have no equivalent single-call cumulative-step histogram, so this returns explicit polyline vertices -- two points per bin edge (a vertical jump to the new cumulative height, then a horizontal run across the bin) -- that any of those backends' plain line/step mark can draw as-is.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>ndarray</code> <p>The array of values to compute the CDF from.</p> required <code>bins</code> <code>int</code> <p>The number of bins to use.</p> required <code>xmin</code> <code>float</code> <p>The lower bound of the x-axis range.</p> required <code>xmax</code> <code>float</code> <p>The upper bound of the x-axis range.</p> required <p>Returns:</p> Type Description <code>tuple[ndarray, ndarray]</code> <p>An <code>(x, y)</code> pair of arrays, each of length <code>2 * bins + 1</code>, tracing the step curve from <code>(xmin, 0)</code> up to <code>(xmax, 1)</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from plotmux.utils.cdf import compute_cdf_steps\n&gt;&gt;&gt; x, y = compute_cdf_steps(np.arange(101), bins=10, xmin=0, xmax=100)\n&gt;&gt;&gt; x.shape, y.shape\n((21,), (21,))\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports","title":"plotmux.utils.imports","text":"<p>Helpers to detect and validate the optional plotting backend dependencies (<code>altair</code>, <code>bokeh</code>, <code>matplotlib</code>, <code>plotly</code>, <code>xy</code>).</p> <p>For each backend, this package re-exports:</p> <ul> <li><code>is_&lt;backend&gt;_available</code>: indicate if the package is installed.</li> <li><code>check_&lt;backend&gt;</code>: raise a <code>RuntimeError</code> if the package is not   installed.</li> <li><code>raise_&lt;backend&gt;_missing_error</code>: raise a <code>RuntimeError</code> with a   message about the missing package.</li> <li><code>&lt;backend&gt;_available</code>: a decorator to conditionally execute a   function only if the package is installed.</li> </ul>"},{"location":"refs/utils/#plotmux.utils.imports.altair_available","title":"plotmux.utils.imports.altair_available","text":"<pre><code>altair_available(fn: F) -&gt; F\n</code></pre> <p>Implement a decorator to execute a function only if <code>altair</code> is installed.</p> <p>Parameters:</p> Name Type Description Default <code>fn</code> <code>F</code> <p>The function to conditionally execute.</p> required <p>Returns:</p> Type Description <code>F</code> <p>A wrapper around <code>fn</code> if <code>altair</code> package is installed, otherwise <code>None</code>.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import altair_available\n&gt;&gt;&gt; @altair_available\n... def my_function(n: int = 0) -&gt; int:\n...     return 42 + n\n...\n&gt;&gt;&gt; my_function()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.bokeh_available","title":"plotmux.utils.imports.bokeh_available","text":"<pre><code>bokeh_available(fn: F) -&gt; F\n</code></pre> <p>Implement a decorator to execute a function only if <code>bokeh</code> is installed.</p> <p>Parameters:</p> Name Type Description Default <code>fn</code> <code>F</code> <p>The function to conditionally execute.</p> required <p>Returns:</p> Type Description <code>F</code> <p>A wrapper around <code>fn</code> if <code>bokeh</code> package is installed, otherwise <code>None</code>.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import bokeh_available\n&gt;&gt;&gt; @bokeh_available\n... def my_function(n: int = 0) -&gt; int:\n...     return 42 + n\n...\n&gt;&gt;&gt; my_function()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.check_altair","title":"plotmux.utils.imports.check_altair","text":"<pre><code>check_altair() -&gt; None\n</code></pre> <p>Check if the <code>altair</code> package is installed.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>if the <code>altair</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import check_altair\n&gt;&gt;&gt; check_altair()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.check_bokeh","title":"plotmux.utils.imports.check_bokeh","text":"<pre><code>check_bokeh() -&gt; None\n</code></pre> <p>Check if the <code>bokeh</code> package is installed.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>if the <code>bokeh</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import check_bokeh\n&gt;&gt;&gt; check_bokeh()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.check_matplotlib","title":"plotmux.utils.imports.check_matplotlib","text":"<pre><code>check_matplotlib() -&gt; None\n</code></pre> <p>Check if the <code>matplotlib</code> package is installed.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>if the <code>matplotlib</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import check_matplotlib\n&gt;&gt;&gt; check_matplotlib()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.check_plotly","title":"plotmux.utils.imports.check_plotly","text":"<pre><code>check_plotly() -&gt; None\n</code></pre> <p>Check if the <code>plotly</code> package is installed.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>if the <code>plotly</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import check_plotly\n&gt;&gt;&gt; check_plotly()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.check_xy","title":"plotmux.utils.imports.check_xy","text":"<pre><code>check_xy() -&gt; None\n</code></pre> <p>Check if the <code>xy</code> package is installed.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>if the <code>xy</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import check_xy\n&gt;&gt;&gt; check_xy()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.is_altair_available","title":"plotmux.utils.imports.is_altair_available  <code>cached</code>","text":"<pre><code>is_altair_available() -&gt; bool\n</code></pre> <p>Indicate if the <code>altair</code> package is installed or not.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>altair</code> is available otherwise <code>False</code>.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import is_altair_available\n&gt;&gt;&gt; is_altair_available()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.is_bokeh_available","title":"plotmux.utils.imports.is_bokeh_available  <code>cached</code>","text":"<pre><code>is_bokeh_available() -&gt; bool\n</code></pre> <p>Indicate if the <code>bokeh</code> package is installed or not.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>bokeh</code> is available otherwise <code>False</code>.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import is_bokeh_available\n&gt;&gt;&gt; is_bokeh_available()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.is_matplotlib_available","title":"plotmux.utils.imports.is_matplotlib_available  <code>cached</code>","text":"<pre><code>is_matplotlib_available() -&gt; bool\n</code></pre> <p>Indicate if the <code>matplotlib</code> package is installed or not.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>matplotlib</code> is available otherwise <code>False</code>.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import is_matplotlib_available\n&gt;&gt;&gt; is_matplotlib_available()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.is_plotly_available","title":"plotmux.utils.imports.is_plotly_available  <code>cached</code>","text":"<pre><code>is_plotly_available() -&gt; bool\n</code></pre> <p>Indicate if the <code>plotly</code> package is installed or not.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>plotly</code> is available otherwise <code>False</code>.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import is_plotly_available\n&gt;&gt;&gt; is_plotly_available()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.is_xy_available","title":"plotmux.utils.imports.is_xy_available  <code>cached</code>","text":"<pre><code>is_xy_available() -&gt; bool\n</code></pre> <p>Indicate if the <code>xy</code> package is installed or not.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>xy</code> is available otherwise <code>False</code>.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import is_xy_available\n&gt;&gt;&gt; is_xy_available()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.matplotlib_available","title":"plotmux.utils.imports.matplotlib_available","text":"<pre><code>matplotlib_available(fn: F) -&gt; F\n</code></pre> <p>Implement a decorator to execute a function only if <code>matplotlib</code> is installed.</p> <p>Parameters:</p> Name Type Description Default <code>fn</code> <code>F</code> <p>The function to conditionally execute.</p> required <p>Returns:</p> Type Description <code>F</code> <p>A wrapper around <code>fn</code> if <code>matplotlib</code> package is installed, otherwise <code>None</code>.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import matplotlib_available\n&gt;&gt;&gt; @matplotlib_available\n... def my_function(n: int = 0) -&gt; int:\n...     return 42 + n\n...\n&gt;&gt;&gt; my_function()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.plotly_available","title":"plotmux.utils.imports.plotly_available","text":"<pre><code>plotly_available(fn: F) -&gt; F\n</code></pre> <p>Implement a decorator to execute a function only if <code>plotly</code> is installed.</p> <p>Parameters:</p> Name Type Description Default <code>fn</code> <code>F</code> <p>The function to conditionally execute.</p> required <p>Returns:</p> Type Description <code>F</code> <p>A wrapper around <code>fn</code> if <code>plotly</code> package is installed, otherwise <code>None</code>.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import plotly_available\n&gt;&gt;&gt; @plotly_available\n... def my_function(n: int = 0) -&gt; int:\n...     return 42 + n\n...\n&gt;&gt;&gt; my_function()\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.raise_altair_missing_error","title":"plotmux.utils.imports.raise_altair_missing_error","text":"<pre><code>raise_altair_missing_error() -&gt; NoReturn\n</code></pre> <p>Raise a <code>RuntimeError</code> to indicate the <code>altair</code> package is missing.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Always, with a message indicating that the <code>altair</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import raise_altair_missing_error\n&gt;&gt;&gt; raise_altair_missing_error()  # doctest: +SKIP\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.raise_bokeh_missing_error","title":"plotmux.utils.imports.raise_bokeh_missing_error","text":"<pre><code>raise_bokeh_missing_error() -&gt; NoReturn\n</code></pre> <p>Raise a <code>RuntimeError</code> to indicate the <code>bokeh</code> package is missing.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Always, with a message indicating that the <code>bokeh</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import raise_bokeh_missing_error\n&gt;&gt;&gt; raise_bokeh_missing_error()  # doctest: +SKIP\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.raise_matplotlib_missing_error","title":"plotmux.utils.imports.raise_matplotlib_missing_error","text":"<pre><code>raise_matplotlib_missing_error() -&gt; NoReturn\n</code></pre> <p>Raise a <code>RuntimeError</code> to indicate the <code>matplotlib</code> package is missing.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Always, with a message indicating that the <code>matplotlib</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import raise_matplotlib_missing_error\n&gt;&gt;&gt; raise_matplotlib_missing_error()  # doctest: +SKIP\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.raise_plotly_missing_error","title":"plotmux.utils.imports.raise_plotly_missing_error","text":"<pre><code>raise_plotly_missing_error() -&gt; NoReturn\n</code></pre> <p>Raise a <code>RuntimeError</code> to indicate the <code>plotly</code> package is missing.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Always, with a message indicating that the <code>plotly</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import raise_plotly_missing_error\n&gt;&gt;&gt; raise_plotly_missing_error()  # doctest: +SKIP\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.raise_xy_missing_error","title":"plotmux.utils.imports.raise_xy_missing_error","text":"<pre><code>raise_xy_missing_error() -&gt; NoReturn\n</code></pre> <p>Raise a <code>RuntimeError</code> to indicate the <code>xy</code> package is missing.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Always, with a message indicating that the <code>xy</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import raise_xy_missing_error\n&gt;&gt;&gt; raise_xy_missing_error()  # doctest: +SKIP\n</code></pre>"},{"location":"refs/utils/#plotmux.utils.imports.xy_available","title":"plotmux.utils.imports.xy_available","text":"<pre><code>xy_available(fn: F) -&gt; F\n</code></pre> <p>Implement a decorator to execute a function only if <code>xy</code> is installed.</p> <p>Parameters:</p> Name Type Description Default <code>fn</code> <code>F</code> <p>The function to conditionally execute.</p> required <p>Returns:</p> Type Description <code>F</code> <p>A wrapper around <code>fn</code> if <code>xy</code> package is installed, otherwise <code>None</code>.</p> Example <pre><code>&gt;&gt;&gt; from plotmux.utils.imports import xy_available\n&gt;&gt;&gt; @xy_available\n... def my_function(n: int = 0) -&gt; int:\n...     return 42 + n\n...\n&gt;&gt;&gt; my_function()\n</code></pre>"},{"location":"uguide/api/","title":"Plotting API","text":"<p> This page describes <code>plotmux</code>'s public plotting functions: <code>plotmux.hist</code>, <code>plotmux.cdf</code>, <code>plotmux.line</code>, <code>plotmux.scatter</code>, <code>plotmux.bar</code>, <code>plotmux.layer</code>, and <code>plotmux.grid</code>. It explains the concepts shared by all of them: specs, backends, common style arguments, and the <code>Figure</code> object they all return.</p> <p>Prerequisites: You'll need to know a bit of Python. For a refresher, see the Python tutorial. It is helpful to know a bit of NumPy.</p>"},{"location":"uguide/api/#overview","title":"Overview","text":"<p><code>plotmux</code> exposes seven top-level plotting functions:</p> <ul> <li><code>plotmux.hist()</code>: plot a histogram</li> <li><code>plotmux.cdf()</code>: plot an empirical cumulative distribution function (CDF)</li> <li><code>plotmux.line()</code>: plot a line chart</li> <li><code>plotmux.scatter()</code>: plot a scatter chart</li> <li><code>plotmux.bar()</code>: plot a bar chart</li> <li><code>plotmux.layer()</code>: combine several charts onto one shared set of axes</li> <li><code>plotmux.grid()</code>: lay out several charts as independent panels in a grid</li> </ul> <p>Each of these functions:</p> <ol> <li>builds a backend-agnostic spec describing what to plot (data + encoding + style),</li> <li>renders that spec through a backend (Matplotlib, <code>xy</code>, ...), resolved from the <code>backend</code>    argument or the current default backend (see Choosing a backend),</li> <li>returns the result wrapped in a <code>Figure</code>.</li> </ol>"},{"location":"uguide/api/#plotting-a-histogram","title":"Plotting a Histogram","text":"<pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 2, 3, 3, 3], bins=10, title=\"My histogram\")\n&gt;&gt;&gt; fig.save(\"histogram.png\")  # doctest: +SKIP\n</code></pre> <p><code>bins</code> controls the number of histogram bins. <code>xmin</code>/<code>xmax</code> control the x-axis range: each can be an explicit numeric value, a quantile string such as <code>\"q0.1\"</code> (the 10% quantile), or <code>None</code> to fall back to the min/max of <code>values</code>:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 2, 3, 3, 3, 100], bins=10, xmin=\"q0.0\", xmax=\"q0.95\")\n&gt;&gt;&gt; fig.save(\"histogram.png\")  # doctest: +SKIP\n</code></pre> <p>Set <code>density=True</code> to normalize the histogram so the area under it integrates to 1.</p>"},{"location":"uguide/api/#plotting-an-empirical-cdf","title":"Plotting an Empirical CDF","text":"<pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.cdf([1, 2, 2, 3, 3, 3], title=\"My CDF\")\n&gt;&gt;&gt; fig.save(\"cdf.png\")  # doctest: +SKIP\n</code></pre> <p><code>plotmux.cdf()</code> plots the empirical cumulative distribution function of <code>values</code>. <code>nbins</code> controls how many bins are used to approximate the curve (<code>None</code> uses the backend's default binning), and <code>xmin</code>/<code>xmax</code> accept the same explicit-value/quantile-string/<code>None</code> semantics as <code>hist</code>. Unlike the other chart functions, <code>ylabel</code> defaults to <code>\"cumulative probability\"</code> instead of <code>None</code>, since a CDF's y-axis always represents that same quantity unless you override it. See Plotting a CDF for a dedicated walkthrough.</p>"},{"location":"uguide/api/#plotting-a-line-chart","title":"Plotting a Line Chart","text":"<pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.line([1, 2, 3], [1, 4, 9], label=\"y = x^2\")\n&gt;&gt;&gt; fig.save(\"line.png\")  # doctest: +SKIP\n</code></pre>"},{"location":"uguide/api/#plotting-a-scatter-chart","title":"Plotting a Scatter Chart","text":"<pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.scatter([1, 2, 3], [1, 4, 9], size=20)\n&gt;&gt;&gt; fig.save(\"scatter.png\")  # doctest: +SKIP\n</code></pre>"},{"location":"uguide/api/#plotting-a-bar-chart","title":"Plotting a Bar Chart","text":"<pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.bar([1, 2, 3], [4, 9, 1], label=\"counts\")\n&gt;&gt;&gt; fig.save(\"bar.png\")  # doctest: +SKIP\n</code></pre> <p><code>width</code> controls the width of each bar, in <code>x</code> data units (defaults to <code>0.8</code>).</p>"},{"location":"uguide/api/#common-style-arguments","title":"Common Style Arguments","text":"<p><code>hist</code>, <code>cdf</code>, <code>line</code>, <code>scatter</code>, and <code>bar</code> all accept the same figure-level style arguments:</p> <ul> <li><code>title</code>: an optional figure title</li> <li><code>xlabel</code> / <code>ylabel</code>: optional axis labels</li> <li><code>xscale</code> / <code>yscale</code>: <code>\"linear\"</code> (default) or <code>\"log\"</code></li> <li><code>color</code>: a hex string (<code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code>), a CSS/Matplotlib named color   (e.g. <code>\"tab:blue\"</code>), or an RGB(A) tuple of floats in <code>[0, 1]</code>. See   Colors for details.</li> <li><code>label</code>: a label used e.g. in the legend</li> </ul> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.line(\n...     [1, 2, 3],\n...     [1, 4, 9],\n...     color=\"tab:orange\",\n...     title=\"Quadratic\",\n...     xlabel=\"x\",\n...     ylabel=\"y\",\n...     yscale=\"log\",\n... )\n&gt;&gt;&gt; fig.save(\"line.png\")  # doctest: +SKIP\n</code></pre>"},{"location":"uguide/api/#backend-specific-arguments","title":"Backend-Specific Arguments","text":"<p>Any extra keyword argument not recognized by the unified API is forwarded as-is to the backend's renderer, letting you reach backend-specific options without leaving the unified API:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 3], bins=5, alpha=0.5)  # \"alpha\" is forwarded to Matplotlib\n&gt;&gt;&gt; fig.save(\"histogram.png\")  # doctest: +SKIP\n</code></pre>"},{"location":"uguide/api/#whats-next","title":"What's Next","text":"<ul> <li>Choosing a Backend: pick a rendering backend, or switch between them</li> <li>Plotting a CDF: a dedicated walkthrough of <code>plotmux.cdf()</code></li> <li>Layering Charts: combine several charts on one set of axes</li> <li>Grid Layouts: lay out several charts as independent panels in a grid</li> <li>Colors: the color formats accepted by <code>color</code> arguments</li> <li>The Figure Object: showing and exporting the rendered figure</li> </ul>"},{"location":"uguide/backends/","title":"Choosing a Backend","text":"<p> This page describes how <code>plotmux</code> resolves which rendering backend to use, how to change the default backend, and how to add a new one.</p>"},{"location":"uguide/backends/#overview","title":"Overview","text":"<p>A backend turns a backend-agnostic spec into a plotting library's native figure object (e.g. a Matplotlib <code>Figure</code>), and knows how to export that native object to a file. <code>plotmux</code> ships with five built-in backends:</p> <ul> <li><code>\"matplotlib\"</code>: renders with Matplotlib (the default). Requires the   <code>matplotlib</code> extra.</li> <li><code>\"xy\"</code>: renders with <code>xy</code>. Requires the <code>xy</code> extra   (Python 3.11+).</li> <li><code>\"bokeh\"</code>: renders with Bokeh. Requires the <code>bokeh</code> extra. Only the   <code>\"html\"</code> export format is supported: static image export (<code>png</code>/<code>svg</code>) would additionally   require a Selenium webdriver at runtime, which is outside the scope of a <code>pip install bokeh</code>.</li> <li><code>\"altair\"</code>: renders with Altair (Vega-Lite). Requires the   <code>altair</code> extra. Only the <code>\"html\"</code>/<code>\"json\"</code> export formats are supported: static image export   (<code>png</code>/<code>svg</code>/<code>pdf</code>) would additionally require the <code>vl-convert-python</code> package, which is outside   the scope of a <code>pip install altair</code>.</li> <li><code>\"plotly\"</code>: renders with Plotly. Requires the <code>plotly</code> extra. Only   the <code>\"html\"</code>/<code>\"json\"</code> export formats are supported: static image export (<code>png</code>/<code>svg</code>/<code>pdf</code>) would   additionally require the <code>kaleido</code> package, which is outside the scope of a <code>pip install plotly</code>.</li> </ul> <p>A backend is only registered if its underlying plotting library is installed, so importing <code>plotmux</code> never fails because a backend's dependency is missing; only calling a plotting function that needs it does.</p>"},{"location":"uguide/backends/#selecting-a-backend-per-call","title":"Selecting a Backend per Call","text":"<p>Every plotting function accepts a <code>backend</code> argument:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 3], bins=5, backend=\"matplotlib\")\n&gt;&gt;&gt; fig.save(\"histogram.png\")  # doctest: +SKIP\n</code></pre> <p><code>backend=None</code> (the default) uses the current default backend.</p>"},{"location":"uguide/backends/#setting-the-default-backend","title":"Setting the Default Backend","text":"<p><code>plotmux.set_backend()</code> changes the default backend for the current thread/task:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; plotmux.set_backend(\"matplotlib\")\n</code></pre>"},{"location":"uguide/backends/#temporarily-overriding-the-default-backend","title":"Temporarily Overriding the Default Backend","text":"<p><code>plotmux.backend()</code> is a context manager that overrides the default backend for the duration of a <code>with</code> block, then restores the previous default:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; with plotmux.backend(\"matplotlib\"):\n...     fig = plotmux.hist([1, 2, 3])\n...\n</code></pre> <p>This is useful to render the same plotting code with several backends, e.g. to compare their output, without permanently changing the default:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; for name in [\"matplotlib\"]:  # doctest: +SKIP\n...     with plotmux.backend(name):\n...         plotmux.line([1, 2, 3], [1, 4, 9]).save(f\"line_{name}.png\")\n...\n</code></pre> <p>The default backend is stored in a <code>contextvars.ContextVar</code>, so it is thread/task-local: setting it in one thread or <code>asyncio</code> task never leaks into, or races with, another one, while still behaving like a single process-wide default in the common single-threaded case.</p>"},{"location":"uguide/backends/#error-handling","title":"Error Handling","text":"<p><code>plotmux.set_backend()</code>/<code>plotmux.backend()</code> reject an unknown name immediately, at the call site, before storing it \u2014 this catches a typo like <code>\"mtaplotlib\"</code> right away instead of only on the next plotting call, at zero import cost:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; plotmux.set_backend(\"not_a_backend\")  # doctest: +SKIP\nTraceback (most recent call last):\n    ...\nRuntimeError: Unknown backend 'not_a_backend'. Known backends: ['altair', 'bokeh', 'matplotlib', 'plotly', 'xy']\n</code></pre> <p>A name that passes this check (a built-in name, or one advertised by an installed third-party plugin) can still fail later, when a plotting function actually renders with it, if its underlying library turns out not to be installed \u2014 being known is not the same as being registered. Passing such a name directly to a plotting function's <code>backend</code> argument (bypassing <code>set_backend</code>/<code>backend</code>) raises the same error there instead:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; plotmux.hist([1, 2, 3], backend=\"not_a_backend\")  # doctest: +SKIP\nTraceback (most recent call last):\n    ...\nRuntimeError: No backend registered under the name 'not_a_backend'. ...\n</code></pre>"},{"location":"uguide/backends/#adding-a-third-party-backend","title":"Adding a Third-Party Backend","text":"<p>A separate package can register a new backend without modifying <code>plotmux</code>'s source, via the <code>plotmux.backends</code> entry-point group. The package declares an entry point pointing at a module that calls <code>register_backend(...)</code> at import time:</p> <pre><code>[project.entry-points.\"plotmux.backends\"]\nmy_backend = \"my_package.plotmux_backend\"\n</code></pre> <pre><code># my_package/plotmux_backend.py\nfrom plotmux.backends.base import Backend\nfrom plotmux.backends.registry import register_backend\n\n\nclass MyBackend(Backend):\n    name = \"my_backend\"\n\n    def render(self, spec, **kwargs): ...\n\n    def save(self, native, path, fmt): ...\n\n\nregister_backend(MyBackend())\n</code></pre> <p><code>plotmux</code> imports every module advertised this way once, when it starts up (<code>import plotmux</code>). Built-in backends, by contrast, are registered lazily: none of the five (<code>matplotlib</code>, <code>xy</code>, <code>bokeh</code>, <code>altair</code>, <code>plotly</code>) is imported at <code>import plotmux</code> time, only the first time that name is actually requested (e.g. via <code>backend=\"matplotlib\"</code> or <code>plotmux.set_backend(\"matplotlib\")</code>). So a third-party plugin module runs before any built-in backend has necessarily registered itself, and can freely reuse a built-in name \u2014 the last one registered under a given name wins.</p> <p>A plugin module that fails to import because its own underlying library is missing (<code>ImportError</code>) is silently skipped, mirroring how the built-in backends guard their own registration behind an \"is this library installed\" check. Any other exception raised while a plugin module is loading (a bug in the plugin itself, e.g. a broken <code>register_backend(...)</code> call) is caught and turned into a <code>RuntimeWarning</code> instead of propagating, so a broken third-party plugin can never crash <code>import plotmux</code> for every user; it can only fail to register itself.</p>"},{"location":"uguide/backends/#whats-next","title":"What's Next","text":"<ul> <li>The Plotting API: the unified plotting functions</li> <li>The Figure Object: per-backend export format support, including the one <code>xy</code>/<code>grid</code>   exception</li> <li>Layering Charts: combine several charts on one set of axes</li> </ul>"},{"location":"uguide/cdf/","title":"Plotting a CDF","text":"<p> This page describes <code>plotmux.cdf()</code>, which plots the empirical cumulative distribution function (CDF) of an array of values.</p>"},{"location":"uguide/cdf/#overview","title":"Overview","text":"<p><code>plotmux.cdf()</code> plots the fraction of <code>values</code> that fall at or below each point on the x-axis, approximated with a binned step curve:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.cdf([1, 2, 2, 3, 3, 3], title=\"My CDF\")\n&gt;&gt;&gt; fig.save(\"cdf.png\")  # doctest: +SKIP\n</code></pre>"},{"location":"uguide/cdf/#binning-and-axis-range","title":"Binning and Axis Range","text":"<p><code>nbins</code> controls how many bins are used to approximate the cumulative curve. <code>None</code> (the default) uses the backend's own default binning:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.cdf([1, 2, 2, 3, 3, 3], nbins=20)\n&gt;&gt;&gt; fig.save(\"cdf.png\")  # doctest: +SKIP\n</code></pre> <p><code>xmin</code>/<code>xmax</code> control the x-axis range, with the same semantics as <code>plotmux.hist()</code>: each can be an explicit numeric value, a quantile string such as <code>\"q0.1\"</code> (the 10% quantile), or <code>None</code> to fall back to the min/max of <code>values</code>:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.cdf([1, 2, 2, 3, 3, 3, 100], xmin=\"q0.0\", xmax=\"q0.95\")\n&gt;&gt;&gt; fig.save(\"cdf.png\")  # doctest: +SKIP\n</code></pre>"},{"location":"uguide/cdf/#style-arguments","title":"Style Arguments","text":"<p><code>cdf</code> accepts the same style arguments as <code>hist</code>, <code>line</code>, and <code>scatter</code>: <code>title</code>, <code>xlabel</code>, <code>ylabel</code>, <code>xscale</code>, <code>yscale</code>, <code>color</code>, <code>label</code>, and <code>backend</code>. The one difference is <code>ylabel</code>, which defaults to <code>\"cumulative probability\"</code> instead of <code>None</code>, since a CDF's y-axis always represents that same quantity unless you override it:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.cdf([1, 2, 2, 3, 3, 3], ylabel=\"fraction of samples\")\n&gt;&gt;&gt; fig.save(\"cdf.png\")  # doctest: +SKIP\n</code></pre>"},{"location":"uguide/cdf/#whats-next","title":"What's Next","text":"<ul> <li>The Plotting API: the other unified plotting functions, and concepts shared by all of   them</li> <li>Layering Charts: e.g. overlay a CDF and a reference line on one set of axes</li> <li>Colors: the color formats accepted by <code>color</code></li> </ul>"},{"location":"uguide/colors/","title":"Colors","text":"<p> This page describes <code>plotmux.colors</code>, which lets every <code>color</code> argument in the unified API accept the same set of color formats, regardless of the backend rendering the figure.</p>"},{"location":"uguide/colors/#overview","title":"Overview","text":"<p>Every <code>color</code> argument in <code>plotmux</code> (<code>plotmux.hist(..., color=...)</code>, <code>plotmux.line(..., color=...)</code>, <code>plotmux.scatter(..., color=...)</code>, <code>plotmux.bar(..., color=...)</code>) accepts one of:</p> <ul> <li>a hex string, <code>\"#rrggbb\"</code> or <code>\"#rrggbbaa\"</code></li> <li>a CSS or Matplotlib named color, e.g. <code>\"tab:blue\"</code>, <code>\"crimson\"</code></li> <li>an RGB(A) tuple of floats in <code>[0, 1]</code></li> </ul> <p>Internally, <code>parse_color()</code> normalizes any of these into one canonical representation, an RGBA tuple of floats in <code>[0, 1]</code>. Each backend then converts that canonical tuple to whatever its native call expects. This lookup table is bundled with <code>plotmux</code> itself (it mirrors Matplotlib's own <code>BASE_COLORS</code>, <code>TABLEAU_COLORS</code>, and <code>CSS4_COLORS</code> tables), so named colors resolve correctly even when Matplotlib is not installed, e.g. when using the <code>xy</code> backend alone.</p>"},{"location":"uguide/colors/#parsing-a-color","title":"Parsing a Color","text":"<pre><code>&gt;&gt;&gt; from plotmux.colors import parse_color\n&gt;&gt;&gt; parse_color(\"#ff0000\")\n(1.0, 0.0, 0.0, 1.0)\n&gt;&gt;&gt; parse_color(\"tab:blue\")\n(0.12156862745098039, 0.4666666666666667, 0.7058823529411765, 1.0)\n&gt;&gt;&gt; parse_color((0.5, 0.5, 0.5))\n(0.5, 0.5, 0.5, 1.0)\n</code></pre> <p>An invalid color raises a <code>ValueError</code>:</p> <pre><code>&gt;&gt;&gt; from plotmux.colors import parse_color\n&gt;&gt;&gt; parse_color(\"not-a-color\")  # doctest: +SKIP\nTraceback (most recent call last):\n    ...\nValueError: ...\n</code></pre>"},{"location":"uguide/colors/#using-colors-in-a-plot","title":"Using Colors in a Plot","text":"<pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.line([1, 2, 3], [1, 4, 9], color=\"#1f77b4\")\n&gt;&gt;&gt; fig.save(\"line.png\")  # doctest: +SKIP\n&gt;&gt;&gt; fig = plotmux.scatter([1, 2, 3], [1, 4, 9], color=(0.9, 0.1, 0.1, 0.8))\n&gt;&gt;&gt; fig.save(\"scatter.png\")  # doctest: +SKIP\n</code></pre> <p>A spec normalizes its <code>color</code> field to the canonical RGBA representation as soon as it is constructed, so every backend always receives an already-validated color and never has to parse a raw color itself.</p>"},{"location":"uguide/colors/#predefined-colors-and-the-default-palette","title":"Predefined Colors and the Default Palette","text":"<p><code>plotmux.colors</code> also exposes a small set of predefined colors, each already a <code>parse_color</code>-normalized RGBA tuple:</p> <pre><code>&gt;&gt;&gt; from plotmux.colors import PRIMARY, SECONDARY, TERTIARY\n&gt;&gt;&gt; PRIMARY\n(0.12156862745098039, 0.4666666666666667, 0.7058823529411765, 1.0)\n</code></pre> <p><code>DEFAULT_PALETTE</code> is an ordered tuple of RGBA tuples used to assign successive, visually-distinct colors to multiple series, or to <code>plotmux.layer()</code> children that set no explicit <code>color</code>:</p> <pre><code>&gt;&gt;&gt; from plotmux.colors import DEFAULT_PALETTE\n&gt;&gt;&gt; len(DEFAULT_PALETTE)\n10\n</code></pre>"},{"location":"uguide/colors/#whats-next","title":"What's Next","text":"<ul> <li>The Plotting API: use <code>color</code> in <code>hist</code>, <code>line</code>, <code>scatter</code>, and <code>bar</code></li> <li>Layering Charts: where <code>DEFAULT_PALETTE</code> disambiguates children without an explicit   color</li> </ul>"},{"location":"uguide/figure/","title":"The Figure Object","text":"<p> This page describes <code>Figure</code>, the object returned by every <code>plotmux</code> plotting function, and how to display, save, or escape to the native figure it wraps.</p>"},{"location":"uguide/figure/#overview","title":"Overview","text":"<p><code>plotmux.hist()</code>, <code>plotmux.line()</code>, <code>plotmux.scatter()</code>, <code>plotmux.bar()</code>, <code>plotmux.layer()</code>, and <code>plotmux.grid()</code> all return a <code>Figure</code>. It keeps three things together:</p> <ul> <li><code>spec</code>: the backend-agnostic spec that was rendered</li> <li><code>backend_name</code>: the name of the backend that rendered it</li> <li><code>native</code>: the backend's native figure object (e.g. a Matplotlib <code>Figure</code>)</li> </ul> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 3], bins=5)\n&gt;&gt;&gt; fig.backend_name\n'matplotlib'\n</code></pre>"},{"location":"uguide/figure/#displaying-a-figure","title":"Displaying a Figure","text":"<p><code>Figure.show()</code> displays the figure using the backend's default viewer:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 3], bins=5)\n&gt;&gt;&gt; fig.show()  # doctest: +SKIP\n</code></pre> <p>If the native object does not support <code>.show()</code>, a <code>NotImplementedError</code> is raised.</p>"},{"location":"uguide/figure/#saving-a-figure","title":"Saving a Figure","text":"<p><code>Figure.save()</code> saves the figure to a file. The export format is inferred from the file suffix (e.g. <code>.png</code>, <code>.svg</code>, <code>.pdf</code>), and the parent directory is created automatically if it doesn't already exist:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 3], bins=5)\n&gt;&gt;&gt; fig.save(\"output/histogram.png\")  # doctest: +SKIP\n</code></pre> <p>A path with no suffix raises a <code>ValueError</code>, since the export format cannot be inferred. Supported formats depend on the backend:</p> Backend Supported formats <code>matplotlib</code> <code>png</code>, <code>svg</code>, <code>pdf</code>, <code>jpg</code>, <code>jpeg</code> <code>xy</code> <code>png</code>, <code>jpg</code>, <code>jpeg</code>, <code>webp</code>, <code>svg</code>, <code>pdf</code>, <code>html</code> <code>bokeh</code> <code>html</code> <code>altair</code> <code>html</code>, <code>json</code> <p>One exception to this table: a <code>plotmux.grid(..., backend=\"xy\")</code> figure only supports <code>\"html\"</code>, even though the <code>xy</code> backend otherwise supports the full row above. <code>xy</code> has no primitive for composing arbitrary, already-built panels side by side, so a grid is laid out at export time as sandboxed HTML iframes rather than a rasterizable native object; see Grid Layouts for details.</p> <p>Requesting an unsupported format raises a <code>ValueError</code> listing the formats the backend supports. Check <code>fig.supported_formats</code> ahead of time to avoid relying on that error for control flow:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 3], bins=5, backend=\"matplotlib\")\n&gt;&gt;&gt; sorted(fig.supported_formats)\n['jpeg', 'jpg', 'pdf', 'png', 'svg']\n</code></pre> <p><code>plotmux.export.save()</code> is the underlying function called by <code>Figure.save()</code>, if you need to call it directly with a <code>Figure</code> you already have.</p>"},{"location":"uguide/figure/#displaying-a-figure-in-jupyter","title":"Displaying a Figure in Jupyter","text":"<p><code>Figure</code> displays itself automatically as the last expression of a Jupyter cell, exactly as its <code>native</code> object would on its own: it forwards the IPython/Jupyter rich-display dunder methods (<code>_repr_html_</code>, <code>_repr_mimebundle_</code>, <code>_repr_svg_</code>, <code>_repr_jpeg_</code>, <code>_repr_png_</code>) to <code>native</code>, so no extra call is needed beyond leaving <code>fig</code> as a cell's last expression. This forwarding is restricted to that fixed set of methods, so a typo like <code>fig.sav(...)</code> still raises a clear <code>AttributeError</code> instead of silently forwarding to <code>native</code>.</p>"},{"location":"uguide/figure/#escaping-to-the-native-figure","title":"Escaping to the Native Figure","text":"<p><code>Figure.to_native()</code> returns the backend's native figure object, the escape hatch to reach backend-specific functionality not exposed by the unified API:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.hist([1, 2, 3], bins=5)\n&gt;&gt;&gt; mpl_fig = fig.to_native()  # a matplotlib.figure.Figure\n</code></pre> <p>Use this when you need a backend-specific feature that <code>plotmux</code>'s unified API does not cover, rather than growing the common API to the union of every backend.</p>"},{"location":"uguide/figure/#whats-next","title":"What's Next","text":"<ul> <li>The Plotting API: the functions that return a <code>Figure</code></li> <li>Choosing a Backend: which backend renders <code>native</code></li> </ul>"},{"location":"uguide/grid/","title":"Grid Layouts","text":"<p> This page describes <code>plotmux.grid()</code>, which lays out several charts as independent panels in a grid.</p>"},{"location":"uguide/grid/#overview","title":"Overview","text":"<p><code>plotmux.grid()</code> accepts any number of child specs, or already-rendered <code>Figure</code>s, and lays each one out in its own, independent panel (the backend-agnostic equivalent of Matplotlib's <code>pyplot.subplots()</code>). This is different from <code>plotmux.layer()</code>, which draws every child onto one shared set of axes: with <code>grid</code>, each child keeps its own axes.</p> <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; from plotmux.specs import HistogramSpec\n&gt;&gt;&gt; fig = plotmux.grid(\n...     HistogramSpec(values=np.arange(101), bins=10),\n...     plotmux.line([0, 100], [0, 5]),\n...     ncols=2,\n...     title=\"Two independent panels\",\n... )\n&gt;&gt;&gt; fig.save(\"grid.png\")  # doctest: +SKIP\n</code></pre> <p>A <code>Figure</code> passed as an item, such as <code>plotmux.line(...)</code> above, is shorthand for its <code>.spec</code>: only the spec is reused. The earlier native figure is discarded and everything is re-rendered together, since two independent native figures can't be merged after the fact in either backend.</p> <p>An item may itself be built with <code>plotmux.layer()</code>, since layering and gridding are independent, composable concerns: a panel can contain several series sharing one set of axes.</p>"},{"location":"uguide/grid/#arranging-panels","title":"Arranging Panels","text":"<p><code>ncols</code> controls the number of columns. Panels fill left to right, top to bottom, in the order the items were given; the last row is left short (its remaining panels left empty) when the number of items is not a multiple of <code>ncols</code>:</p> <pre><code>&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; fig = plotmux.grid(\n...     plotmux.line([1, 2, 3], [1, 2, 3]),\n...     plotmux.line([1, 2, 3], [3, 2, 1]),\n...     plotmux.line([1, 2, 3], [1, 4, 9]),\n...     ncols=2,\n... )\n&gt;&gt;&gt; fig.save(\"grid.png\")  # doctest: +SKIP\n</code></pre>"},{"location":"uguide/grid/#style-arguments","title":"Style Arguments","text":"<p>Unlike <code>hist</code>, <code>line</code>, <code>scatter</code>, and <code>layer</code>, <code>grid</code> only accepts <code>title</code> and <code>backend</code>, plus <code>ncols</code>. <code>title</code> is shown once above the whole grid, not any individual panel. <code>xlabel</code>, <code>ylabel</code>, <code>xscale</code>, <code>yscale</code>, and <code>color</code> have no meaning at the grid level since each panel keeps its own style: set those on the child specs themselves instead.</p>"},{"location":"uguide/grid/#restrictions","title":"Restrictions","text":"<p><code>grid</code> does not support nesting: passing a <code>GridSpec</code> (or a <code>Figure</code> built from one) as one of its items raises a <code>ValueError</code>. Layout is one flat pass over <code>cells</code>, so flatten nested grids yourself before calling <code>grid</code> rather than relying on recursive dispatch. <code>grid</code> also requires at least one item, and <code>ncols</code> must be a positive integer.</p>"},{"location":"uguide/grid/#the-xy-backend-is-html-only-for-grids","title":"The <code>xy</code> Backend Is HTML-Only for Grids","text":"<p><code>xy</code> has no primitive for arranging arbitrary, already-built, independent panels side by side (its <code>facet_chart</code> repeats one fixed chart per value of a data column, which is a different thing), so <code>grid(..., backend=\"xy\")</code> lays out each panel's standalone chart as a sandboxed HTML <code>&lt;iframe&gt;</code> at export time instead of producing a single rasterizable native figure. As a result, <code>fig.save(...)</code> only accepts an <code>.html</code> path for an <code>xy</code> grid; every other format <code>xy</code> otherwise supports (<code>png</code>, <code>svg</code>, <code>pdf</code>, ...) raises a <code>ValueError</code> for this one figure shape. Every other backend (<code>matplotlib</code>, <code>bokeh</code>, <code>altair</code>, <code>plotly</code>) supports its full usual set of formats for <code>grid()</code> too. See The Figure Object for the full per-backend format table.</p>"},{"location":"uguide/grid/#whats-next","title":"What's Next","text":"<ul> <li>The Plotting API: the unified plotting functions combined by <code>grid</code></li> <li>Layering Charts: combine several charts onto one shared set of axes, for use as a   single grid panel</li> </ul>"},{"location":"uguide/layer/","title":"Layering Charts","text":"<p> This page describes <code>plotmux.layer()</code>, which combines several charts onto one shared set of axes.</p>"},{"location":"uguide/layer/#overview","title":"Overview","text":"<p><code>plotmux.layer()</code> accepts any number of child specs, or already-rendered <code>Figure</code>s, and draws them together on one shared set of axes, e.g. overlaying a line on top of a histogram:</p> <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; import plotmux\n&gt;&gt;&gt; from plotmux.specs import HistogramSpec\n&gt;&gt;&gt; fig = plotmux.layer(\n...     HistogramSpec(values=np.arange(101), bins=10),\n...     plotmux.line([0, 100], [0, 5]),\n...     title=\"Histogram with a reference line\",\n... )\n&gt;&gt;&gt; fig.save(\"layered.png\")  # doctest: +SKIP\n</code></pre> <p>A <code>Figure</code> passed as an item, such as <code>plotmux.line(...)</code> above, is shorthand for its <code>.spec</code>: only the spec is reused. The earlier native figure is discarded and everything is re-rendered together, since two independent native figures can't be merged after the fact in either backend.</p>"},{"location":"uguide/layer/#style-arguments","title":"Style Arguments","text":"<p>Like <code>hist</code>, <code>line</code>, and <code>scatter</code>, <code>layer</code> accepts <code>title</code>, <code>xlabel</code>, <code>ylabel</code>, <code>xscale</code>, <code>yscale</code>, and <code>backend</code>. These describe the combined axes, not any individual child: a child's own <code>title</code> (if it has one) is ignored when it is drawn as part of a layer.</p>"},{"location":"uguide/layer/#colors-in-a-layer","title":"Colors in a Layer","text":"<p>A child spec that sets no explicit <code>color</code> is assigned one from <code>DEFAULT_PALETTE</code> (see Colors), in draw order, so each child remains visually distinguishable without you having to pick colors by hand.</p>"},{"location":"uguide/layer/#restrictions","title":"Restrictions","text":"<p><code>layer</code> does not support nesting: passing a <code>LayerSpec</code> as one of its items raises a <code>ValueError</code>. Layering is one flat pass over its children, so flatten nested layers yourself before calling <code>layer</code> rather than relying on recursive dispatch. <code>layer</code> also requires at least one item.</p>"},{"location":"uguide/layer/#whats-next","title":"What's Next","text":"<ul> <li>The Plotting API: the unified plotting functions combined by <code>layer</code></li> <li>Grid Layouts: lay out several charts (or layers) as independent panels in a grid</li> <li>Colors: how colors are assigned to layer children</li> </ul>"}]}